# NAI Auto Generator V5 패치 노트

새 버전을 낼 때 이 파일 맨 위에 `## v버전 (날짜)` 절을 추가합니다. 릴리스 워크플로가 태그와
같은 이름의 절을 뽑아 릴리스 본문에 넣으므로, **절이 없으면 릴리스가 실패합니다**
(`tests/core/test_patchnotes.py`가 CI에서 먼저 잡습니다).

---

## v0.9.3 (2026-09-11)

작은 UI 손질 하나와, 겉으로 보이는 동작은 그대로인 내부 정리입니다. 코드 전반을 훑어
중복을 걷어내고 큰 파일에서 로직을 떼어 냈습니다.

### ✨ 개선

- **`해상도 랜덤` 체크를 해상도 섹션으로 옮겼습니다.** 생성 바에 떨어져 있어 해상도
  설정과 나뉘어 있던 것을, 너비·높이 입력 바로 아래로 모았습니다. 이제 켜고 끄는 것이
  연속 생성 도중에도 다음 이미지부터 반영됩니다 (다른 해상도 조작과 같은 규칙).

### 🧹 내부 정리

- **아레나 네 탭의 그림 로딩을 한 곳으로 모았습니다.** 월드컵·판독·조합 생성·결산 탭이
  저마다 그림을 디스크에서 다시 읽던 것을, 수정 시각 기반 캐시를 갖춘 공용 로더 하나로
  합쳤습니다. 조합이 많은 순위표에서 표를 다시 그릴 때 매번 다시 디코딩하지 않습니다.
- **세팅별 연속 생성의 상태 관리를 메인 창에서 떼어 냈습니다.** 순환 진행·다음 파일 선택
  판정을 화면과 분리된 별도 부품으로 옮겨, 메인 창이 얇아지고 이 부분을 단독으로 시험할
  수 있게 됐습니다.
- **프롬프트 조립 규칙을 모델 스펙 한 곳에 뒀습니다.** 품질 태그와 UC 프리셋을 붙이는
  규칙이 세 곳에 흩어져 있던 것을 모델 스펙의 메서드로 통합해, 한 번 생성과 연속 생성
  도중 실시간 반영이 어긋날 여지를 없앴습니다.

### 🐛 버그 수정

- **문자 그림(캐릭터 레퍼런스)과 마스크를 만들 때 열던 이미지 핸들을 확실히 닫습니다.**
  배치로 여러 장을 만들 때 파일 핸들이 쌓이던 경로를 막았습니다.
- **와일드카드 폴더를 생성 사이클마다 한 번만 읽습니다.** 이전에는 한 장을 만들 때마다
  폴더 전체를 다시 훑어, 와일드카드 파일이 많고 여러 장을 뽑을수록 느려졌습니다.

---

## v0.9.2 (2026-09-11)

작은 손질 두 가지입니다.

### ✨ 개선

- **조합 생성 탭의 조합 큐에서 우클릭 메뉴가 생겼습니다.** 골라 둔 작가 조합에 대고
  오른쪽 버튼을 누르면 `수정`·`복사`·`삭제`를 바로 할 수 있습니다.
  - **수정** — 작가 블록을 완성 프롬프트 문자열 그대로 고칩니다. 가중치 문법
    (`1.2::artist:name::`)을 그대로 쓸 수 있고, 그림과 전적은 그대로 둔 채 작가 구성만
    바뀝니다 (조합을 하나만 골랐을 때 켜집니다).
  - **복사** — 고른 조합의 작가 블록을 클립보드로 보냅니다 (여러 개면 줄바꿈으로 잇습니다).
  - **삭제** — 기존 `선택 삭제`와 같습니다 (되돌리기로 되살릴 수 있고, 생성 중에는 막힙니다).
  - **단축키** — 큐 표에 포커스가 있을 때 `F2`로 수정, `Ctrl+C`로 복사, `Delete`로 삭제할
    수 있습니다.

- **그림체 아레나를 여는 단축키가 생겼습니다.** 메뉴 `도구 → 그림체 아레나`에
  `Ctrl+Shift+A`가 붙어, 마우스 없이 바로 열 수 있습니다.
- **아레나 창 안에서 탭을 단축키로 넘깁니다.** `Ctrl+1`~`Ctrl+5`로 각 탭(명단·생성·
  월드컵·LLM 추천·결과)으로 바로 가고, `Ctrl+Tab` / `Ctrl+Shift+Tab`으로 다음·이전
  탭을 돕니다. 탭 머리에 마우스를 올리면 어떤 키인지 알려 줍니다. 월드컵 탭의 방향키
  판정과는 겹치지 않습니다(그쪽은 Ctrl 없는 화살표·Space).

### 🐛 버그 수정

- **프롬프트 입력 중 태그 자동완성창이 사라지지 않고 남던 문제를 고쳤습니다.** 자동완성
  팝업은 입력창과 별개의 창이라, 다른 곳을 클릭해 입력창에서 포커스가 빠지거나 글자를
  바꾸지 않고 커서만 옮겼을 때 그 자리에 그대로 떠 있었습니다. 이제 포커스가 빠지거나
  입력창이 숨겨지면 팝업이 함께 닫히고, 커서를 옮기면 그 자리의 태그에 맞춰 다시
  맞춰집니다.

---

## v0.9.0 (2026-09-10)

**그림체 아레나 화면을 통째로 손봤습니다.** 기능은 그대로인데 여섯 탭이 전부 "설정 폼 →
표 → 버튼 줄"을 세로로만 쌓아 올려, 지금 어디까지 왔는지도 다음에 뭘 눌러야 하는지도
화면이 말해 주지 않았습니다. 배치·위계·빈 화면 세 가지를 정리했습니다.

### ✨ 개선 — 창 전체

- **상태줄이 생겼습니다.** 창 맨 아래에 어느 탭에서나 같은 자리로
  `작가 15 · 조합 24 (그림 18) · 대결 142회 · 미확정 4`가 뜨고, 옆의 **`다음`** 버튼이
  지금 할 차례인 단계의 탭으로 데려다줍니다. **대신 눌러 주지는 않습니다** — 단계 중에
  크레딧을 쓰는 것(그림 뽑기)이 섞여 있어서입니다. 생성 진행 막대와 한 줄 설명도 여기로
  모았습니다 (메인 창의 상태 표시줄과 같은 쪽입니다).
- **버튼에 등급이 생겼습니다.** 화면마다 주 동작 하나만 칠하고, 되돌릴 수 없는 것
  (`명단 비우기` · `삭제` · `정리` · `초기화`)은 떼어 놓고 붉게 표시합니다. 색은 시스템
  팔레트에서 가져오므로 밝은·어두운 테마를 그대로 따릅니다.
- **빈 화면이 막다른 길이 아닙니다.** 왜 못 하는지를 원인별로 말하고, 그것을 푸는 자리로
  가는 버튼을 함께 줍니다.

### ✨ 개선 — 탭별

- **이상형 월드컵** — 그림에 화면을 몰아줬습니다. `← → 승자 · ↑ 둘 다 좋음 …` 안내문
  한 줄이 없어지고 **단축키가 버튼 라벨 안으로** 들어갔습니다 (단축키를 끄면 표기도
  사라집니다). 진행 상황은 지금 보고 있는 리그가 얼마나 확정됐는지를 막대로 보여 주고,
  티어 글자에 색이 붙었습니다. 겨룰 그림이 없을 때는 원인을 넷으로 구분해 말합니다
  (조합 없음 / 그림 대기 / 상대 부족 / 리그가 좁음).
- **조합 생성** — 좌우로 나눴습니다. 왼쪽은 조작값(규칙·뽑기·기본 프롬프트를 접어 두고
  접힌 동안에는 현재 값을 요약 한 줄로), 오른쪽은 조합 큐입니다. **큐에 썸네일이 붙어**
  뽑아 둔 그림을 이 탭에서 볼 수 있고, 상태 칸에 기호가 붙었습니다(`○ 대기` · `✔ 그림 있음`).
  크레딧을 쓰는 유일한 버튼인 `그림 생성`은 칠하고 **라벨에 장수를 적습니다**.
- **`통계` + `그림체 개선` → `결과` 한 탭** — 둘이 같은 목록을 두 화면에서 보고 있었습니다
  (개선 탭의 "부모 후보"는 티어 시트 상위 N개와 사실상 같습니다). 왼쪽은 순위표 하나,
  오른쪽은 **고른 조합의 그림과 프롬프트**입니다 — 통계 탭에서는 태그 문자열만 보여
  "이게 어떤 그림이었지"를 알 수 없었습니다. 티어·세대 분포는 막대로 바뀌었고, 교배·결산·
  정리·기록·작가 순위는 접이식으로 내려갔습니다.
- **작가 명단** — 왼쪽이 순위표, 오른쪽이 넣는 자리입니다. 끌어다 놓는 자리가 **실제로
  보이는 점선 과녁**이 되고(끌고 오는 동안 밝아집니다), **이름 검색**이 생겼습니다.
  뜻을 짐작하기 어렵던 라벨도 고쳤습니다: `검출` → `조합 등장`, `_ → 공백` → `이름 다듬기`.
- **LLM 추천** — 참조는 왼쪽에 고정하고 랭킹에 오른쪽을 통째로 줬습니다. 랭킹 줄마다
  **썸네일**이 붙고, 점수를 **막대로도** 보여 줍니다 — 숫자만 늘어놓으면 90점대에 몰리는
  포화가 눈에 띄지 않는데, 그때가 판독 지시를 손봐야 할 때입니다. 참조 그림은 끌어다
  놓아서도 넣습니다.

### ✨ 새 기능

- **판독한 모델과 조건이 함께 남습니다.** 모델을 바꿔 가며 어느 쪽이 사람 취향과 잘 맞는지
  견주려면 점수만으로는 모자랍니다 — 참조를 몇 장 썼는지, 채점 지시를 손봤는지에 따라
  점수가 달라지기 때문입니다.
  - 점수마다 **그 점수를 매긴 모델 이름**이 붙습니다. 설정에 적어 둔 이름이 아니라 서버가
    **실제로 고른** 식별자입니다 (모델 칸을 비워 두면 로드된 첫 모델이 쓰이고, 값을 적어도
    부분 일치로 골라질 수 있습니다). 판독이 끝나면 상태줄이 그 이름을 알려 줍니다.
  - 한 회차의 조건(모델·주소·타임아웃·참조 장수·상한·채점 지시 수정 여부·성공/실패 수·
    시각)이 `arena.json`에 저장되어 앱을 껐다 켜도 남습니다.
  - LLM 추천 탭의 **`결과 저장에 판독 조건도 함께`**(기본 켜짐)를 켜 두면 CSV 줄마다
    `model` 칸이 붙고, 데이터 아래에 조건 구역이 따라옵니다. 여러 모델의 CSV를 **이어 붙여**
    모델별로 묶어 보라고 조건이 아니라 줄마다 모델을 적었고, 조건 항목 이름은 번역하지
    않습니다 (언어를 바꿔 뽑은 파일이 다른 열 이름을 가지면 이어 붙일 수 없습니다).
  - `점수 비우기`는 모델 이름과 조건 기록도 함께 지웁니다.

### 🐛 버그 수정

- **`랜덤 조합 만들기`가 그림 생성을 시작하던 문제** — `그림 자동 보충`을 켜 두면, 조합
  생성 탭에서 조합을 만드는 것만으로 **크레딧을 쓰는 생성이 걸렸습니다.** 월드컵 탭이
  조합 변화에 따라 다시 그려지며 자동 보충 경로까지 흘러갔기 때문입니다. 이제 자동 보충은
  **월드컵 탭을 보고 있을 때만** 나갑니다.
- **생성 중에 조합을 더 만들 수 없던 문제** — 처음에는 조합을 여러 번 나눠 만드는데,
  첫 묶음에서 생성이 시작되면 `랜덤 조합 만들기`까지 잠겼습니다. 만드는 것은 공짜이고
  돌고 있는 잡은 시작할 때 뜬 목록만 보므로, 이제 만들기는 열어 두고 **지우는 것만** 막습니다.

### ⚠️ 알아 두세요

- 아레나 탭이 **여섯 개에서 다섯 개로** 줄었습니다 (`통계`와 `그림체 개선`이 `결과`로
  합쳐졌습니다). 하던 일과 데이터는 그대로입니다.
- 판독 결과 CSV의 열이 넷에서 **다섯으로** 늘었습니다 (`model` 추가). 예전 네 칸이 필요하면
  LLM 추천 탭의 `결과 저장에 판독 조건도 함께`를 끄세요.
- `arena.json`은 **더하기만** 했습니다 — 예전 파일도 그대로 읽히고, 이전 버전으로 되돌려도
  새로 붙은 항목만 무시됩니다.

> 아레나 전체 사용법은 [docs/ARENA.md](docs/ARENA.md)에 있습니다.

---

## v0.8.5 (2026-09-09)

그림체 아레나에 **LLM 추천** 탭이 들어왔습니다. V4에서 쓰던 그림체를 V5에서 재현하려고
무작위 조합을 잔뜩 뽑아 놓았을 때, 로컬 VLM(LM Studio)에게 "어느 후보가 V4 참조 그림체와
가장 닮았는지"를 자동으로 점수 매기게 해 1차 선별을 맡깁니다. 사람이 직접 겨루는 이상형
월드컵을 대체하는 것이 아니라, 경우의 수가 너무 많을 때 후보를 좁혀 주는 도구입니다.

### ✨ 새 기능

- **LLM 추천 (그림체 자동 판독)** — 아레나 창의 새 탭입니다.
  - **참조 그림체 등록** — V4에서 뽑아 둔 이미지를 여러 장 목표로 등록합니다. `판독 실행`을
    누르면 그림이 있는 후보를 한 장씩 로컬 VLM에 넣어 0~100 유사도 점수와 근거를 받아,
    점수순으로 줄 세웁니다. **NovelAI 크레딧을 쓰지 않습니다** — 이미 뽑아 둔 그림을 읽어
    보내는 것이라, 메인·아레나 생성이 도는 중에도 돌릴 수 있습니다.
  - 프롬프트 어시스턴트(`Ctrl+G`)와 같은 LM Studio 연결을 씁니다. 이미지를 봐야 하므로
    **비전(VLM) 모델**이 필요하고, 참조를 여러 장 넣으므로 다중 이미지를 지원하는 모델을
    권합니다. 판독에 쓸 모델은 이 탭에서 따로 고를 수 있습니다.
  - **후속 액션** — 상위 N개를 즐겨찾기로 올려 월드컵의 `즐겨찾기` 리그에서 사람이 최종
    비교하거나, 고른 조합을 클립보드로 복사합니다. **결과 저장**으로 랭킹을 CSV 파일로
    내보내고(엑셀에서 한글도 깨지지 않게), **점수 비우기**로 판독 점수만 초기화해 참조를
    바꿔 처음부터 다시 판독할 수 있습니다 (사람 월드컵 점수·전적은 그대로 남습니다).
- **통계 탭에 판독 점수 반영** — 티어 시트에 `LLM` 점수 칸이 뜨고, 사람 Elo와 나란히
  대조할 수 있습니다. `Elo 점수순`과 `LLM 점수순` 정렬을 고르고, 결산도 `LLM 점수 순`으로
  돌려 월드컵을 안 돌렸어도 LLM 추천 순서 그대로 실제 그림을 뽑을 수 있습니다.

### ✨ 개선

- **판독 지시(시스템 프롬프트) 직접 편집** — 판독 점수가 90점 이상에 몰려 순위가
  무의미해질 때(점수 포화), LLM 추천 탭에서 채점 지시를 직접 손볼 수 있습니다. 기본
  프롬프트에는 점수대별 기준과 "엄격하게, 넓게 분포시켜라"는 지시를 넣어 포화를 줄였습니다.
  다만 점수 포화의 주 원인은 모델의 판별력이라, 계속 몰리면 더 큰 비전 모델로 바꾸는 것을
  먼저 권합니다.

> 자세한 사용법은 [docs/ARENA.md](docs/ARENA.md)의 *LLM 추천* 절을 보세요.

---

## v0.8.1 (2026-09-09)

**해상도 랜덤**이 커스텀 해상도를 제대로 씁니다.

### 🐛 버그 수정

- **커스텀 해상도로 골라 둔 등급이 되돌아가던 문제** — 옵션을 저장하거나, 모델을 바꾸거나,
  세팅별 연속 생성이 세팅 파일을 갈아 끼우거나, 프로그램을 다시 켤 때마다 해상도 등급이
  조용히 `Normal`로 돌아갔습니다. 그 상태에서 `해상도 랜덤`을 켜 두면 커스텀이 아닌 일반
  해상도가 뽑혔습니다. 커스텀 행에는 보통 Normal에도 있는 크기(1024×1024 같은)를 적어 두는데,
  프로그램이 **크기만 보고 등급을 되짚었기** 때문입니다. 이제 고른 등급을 기억하고 설정·세팅
  파일에도 함께 적어 둡니다 — Large·Wallpaper 등급도 마찬가지입니다.

### ✨ 개선

- **해상도 랜덤이 커스텀 해상도 전부를 후보로 씁니다** (V4와 같은 규칙). 등급이 `커스텀`이면
  옵션에서 켜 둔 커스텀 해상도 6칸 중 켜진 것 **전부** 중에서 매 장 하나를 고릅니다. 전에는
  세로/가로/정방형 대표 하나씩만 뽑혀, 같은 비율로 여러 개를 등록해도 첫 줄만 나왔습니다.
  그 밖의 등급은 지금까지처럼 Wide/Square/Portrait 중 하나를 고릅니다.

---

## v0.8.0 (2026-09-08)

**그림체 아레나**가 들어왔습니다. 작가 태그를 무작위로 조합해 그림을 뽑고, 둘씩 겨루게 해
취향을 점수로 남기고, 이긴 조합끼리 교배해 더 마음에 드는 그림체를 찾아 갑니다.
도구 → `그림체 아레나...`로 엽니다.

순위가 나온 뒤가 더 편해졌습니다 — 위에서부터 실제 그림을 뽑는 **결산 생성**, 한 벌을 통째로
담아 두는 **기록 내보내기·불러오기**, 그리고 통계를 비우는 자리를 함께 넣었습니다. 지운 그림과
프리셋은 이제 **휴지통으로** 갑니다.

### ✨ 새 기능

- **그림체 아레나** — 다섯 탭으로 나뉘어 있습니다.
  - **작가 명단** — 조합에 뽑힐 후보이자, 그 자체로 순위표입니다. 조합이 겨룰 때마다 거기
    낀 작가도 점수를 나눠 받아, 여러 조합에 걸쳐 꾸준히 이긴 작가가 위로 올라옵니다.
    가중치가 붙은 완성 프롬프트를 통째로 붙여 넣어도 작가 태그만 골라 등록하고, **그림
    파일을 끌어다 놓으면 그 그림에 쓰인 작가를 명단에 넣습니다** (PNG · WebP · 스텔스
    메타데이터 모두 읽습니다). 여러 줄을 골라 한 번에 지우고(`Delete`), 표에서 이름을 바로
    고치고, `_`를 공백으로 일괄 정리할 수 있습니다. 특정 작가의 가중치를 고정해 두면
    조합할 때마다 항상 그 세기로 들어갑니다.
  - **조합 생성** — 작가 수·가중치 방식(랜덤/균형/커브)·**가중치 범위와 증감 폭**
    (0.01~1 단위)·넣을 위치를 정해 조합을 만들고, `그림 생성`으로 그림이 없는 조합의
    그림을 뽑습니다. 만든 조합은 **큐 목록**에 쌓이고, 필요 없는 것은 골라서 지웁니다
    (`대기 중인 조합 모두 삭제`도 있습니다). 미리보기는 실제로 보낼 조건을 그대로
    보여 줍니다 — 작가 조합 · 프롬프트 · 네거티브 · 캐릭터 프롬프트. **한 세션의 모든 조합은 같은 시드·같은 기본
    프롬프트로 뽑힙니다** — 조합마다 시드가 달라지면 구도와 포즈가 함께 바뀌어 그림체
    차이를 덮어 버리기 때문입니다. 기본 프롬프트는 메인 창의 것을 그대로 쓰고, 거기에
    매 장 달라지는 문법(와일드카드 등)이 있으면 알려 줍니다.
  - **이상형 월드컵** — 두 그림을 나란히 놓고 고릅니다. `둘 다 좋음` · `비김` ·
    `둘 다 아쉬움` · `넘기기` · `양쪽 삭제`가 있고, 방향키로도 됩니다
    (`←` `→` 승자 · `↑` 둘 다 좋음 · `↓` 둘 다 아쉬움 · `Space` 넘기기 · `Delete` 양쪽 삭제).
    **그림을 누르면 크게 보입니다** — 고르는 것은 그림 아래 버튼과 방향키입니다.
    배율 슬라이더(50~250%)로 그림 크기를 키울 수 있고, 리그(전체 / 즐겨찾기 / 미확정만)를
    골라 원하는 범위만 겨루게 할 수 있습니다.
  - **그림체 개선** — 상위 조합을 교배해 다음 세대를 만듭니다. 부모 후보는 **썸네일과
    함께** 보여 줍니다. 조건에 맞는 조합을 한 번에 정리하거나, 취향이 아닌 작가가 낀 조합을
    작가 단위로 지울 수 있습니다 (지우기 전에 대상 개수를 보여 주고 확인을 받습니다).
  - **통계** — 티어 시트(S~F) · 작가 순위 · 세대 분포. 시트에서 조합을 골라 **메인 창
    프롬프트로 보내거나** 클립보드로 복사합니다.
- **아레나 폴더** — 데이터는 결과 폴더와 나눠 `artist_arena/`에 둡니다 (`arena.json` +
  `images/`). 아레나가 뽑는 그림은 습작이라 결과 폴더에 섞이면 갤러리가 습작으로 덮입니다.
  경로는 옵션 → 폴더 경로에서 바꾸고, 폴더 메뉴에서 바로 엽니다.

### 🔬 점수와 진화

같은 부류의 프로그램들과 다르게 잡은 곳들입니다.

- **적응형 K** (32 → 24 → 16). 초반에는 점수가 크게 움직여 빨리 제자리를 찾고, 판수가
  쌓이면 잘 흔들리지 않습니다. 이 값이 티어 `확정` 판정의 근거이기도 해서, 몇 판 안 싸운
  1300점은 `S?`로 표시됩니다.
- **덜 싸운 조합 우선 + 점수가 가까운 상대**로 대진을 짭니다. 승부가 뻔한 대결이 줄어
  같은 클릭 수로 순위가 더 정확해집니다.
- **작가 점수는 자리 가중치에 비례해** 나눕니다. 1.8로 들어간 작가가 0.8로 들어간 작가보다
  결과에 더 책임이 있습니다.
- **가중치도 유전합니다.** 교배할 때 작가 이름만 물려주고 가중치를 새로 뽑으면 "이 작가를
  세게 쓴 게 좋았다"는 정보가 매 세대 사라집니다.
- **자식은 부모 점수의 평균에서** 시작합니다. 아직 아무도 보지 않은 자식이 검증된 부모보다
  위에 앉지 않습니다.

### 🛡️ 안전장치

- 생성은 **기존 생성 파이프라인에 위임**합니다. 메인 배치가 도는 중에는 아레나가 시작되지
  않고, 반대도 마찬가지입니다 (NovelAI는 계정 단위로 요청을 하나씩 보내는 것이 규칙입니다).
- **그림 자동 보충은 기본이 꺼짐**입니다. 겨룰 그림이 떨어졌을 때 알아서 더 뽑는 편이
  편하지만 그것은 실제로 크레딧을 쓰는 동작이라, 켜지 않았으면 안내만 합니다.
- 그림은 **한 장 뽑을 때마다 바로 기록**합니다. 도중에 앱이 죽어도 이미 쓴 크레딧이
  날아가지 않습니다.
- **지운 것은 되돌릴 수 있습니다.** 조합을 지워도 그림 파일은 기본적으로 남기므로,
  `되돌리기` 한 번이면 점수·전적·그림이 모두 돌아옵니다 (큐·월드컵·진화 어디서 지웠든).
  파일까지 지우고 싶으면 `그림 파일도 함께 삭제`를 켜면 되고, 남은 파일은 통계 탭의
  `안 쓰는 그림 정리`로 치웁니다. 그림이나 전적이 딸린 조합을 지울 때는 먼저 묻습니다.
- **대전 기록 초기화와 작가 명단 초기화는 따로**입니다. 점수만 지우고 명단·조합·그림은
  남길 수 있습니다.

### 🧹 통계 초기화

`대전 기록 초기화`를 눌러도 세대 분포와 조합 목록이 그대로 남는다는 제보를 받았습니다. 그쪽은
**점수만 0으로 되돌리고 조합은 남기는** 동작이라 의도한 대로였지만, "통계를 통째로 비우고
싶다"는 자리가 없었습니다. 통계 탭에 `통계 초기화`를 넣었습니다.

| 버튼 | 지우는 것 | 남는 것 |
|---|---|---|
| 대전 기록 초기화 | 점수 · 전적 | 작가 명단 · **조합** · 그림 |
| **통계 초기화** | 점수 · 전적 · **조합 목록** · 티어 시트 · 세대 분포 | 작가 명단 |

- 누르면 **무엇이 지워지고 무엇이 남는지** 개수와 함께 보여 주고, **`기록 내보내기`로 먼저
  백업하라고** 안내합니다.
- 조합 삭제는 서비스를 거치므로, 실수로 눌렀어도 조합 생성 탭의 `되돌리기`로 되살아납니다
  (그림 파일도 기본값에서는 그대로 남습니다). 확인 창에 그 사실과 한계도 적어 뒀습니다.
- 작가 명단은 남깁니다 — 애써 모은 후보까지 날아가면 곤란하다는 것이 원래 둘을 나눈
  이유입니다. 명단을 비우려면 작가 명단 탭의 `전체 삭제`를 쓰세요.

### 🏁 통계 결산 이미지 생성

순위가 나왔으면 다음 할 일은 정해져 있습니다 — **위에서부터 차례로 실제 그림을 뽑아 한 벌로
보는 것.** 조합을 하나씩 골라 프롬프트에 붙이고 누르기를 반복하지 않아도 되게, 통계 탭에
`결산 생성`을 넣었습니다.

- **상위 N개** · **조합당 M장**을 정하면 순위 위에서부터 큐를 만들어 한 번에 돌립니다.
  1등을 M장 다 뽑고 2등으로 넘어가므로, 결과 폴더에 **등수 순서 그대로** 쌓입니다.
- 뽑을 장수(`대상 조합 N개 · 뽑을 그림 M장`)가 버튼 옆에 늘 떠 있고, 누르면 한 번 더
  확인합니다 — 크레딧을 쓰는 동작이라서입니다.
- **결과는 결과 폴더에 저장됩니다** (아레나 폴더의 습작과 다른 자리). 갤러리와 결과 미리보기,
  파일명 규칙, 메타데이터까지 평소 생성과 완전히 같습니다 — 메인 창의 기존 생성 경로를
  그대로 쓰기 때문입니다.
- 전적이 없는 조합은 기본으로 빠집니다. 1000점은 실력이 아니라 '모름'이라 결산에 섞이면
  순위가 있는 것처럼 보입니다 (`전적 없는 조합도` 체크로 포함할 수 있습니다).
- 시드는 메인 창의 `시드 랜덤` 설정을 따릅니다. 꺼 두면 **같은 시드로 그림체만** 바뀐 한 벌이
  나와, 월드컵과 같은 방식으로 비교할 수 있습니다.

### 🖥️ 낮은 해상도에서 창이 잘리던 문제

1366x768 같은 화면에서 아레나 창의 윗부분이 잘려 보인다는 제보를 고쳤습니다.

- **원인**: 월드컵 카드가 배율만큼의 높이를 그대로 `최소`로 걸고 있었습니다. 배율 250%면
  카드 하나가 900px를 요구했고, 두 장이 나란히 서니 창을 화면보다 크게 만들어야 해서
  아래위가 잘렸습니다. 이제 배율은 **최대**만 정하고 최소는 160px에 묶여, 배율을 아무리
  올려도 창 최소 크기가 커지지 않습니다.
- 창의 최소 크기 자체도 **화면을 넘지 않게** 맞춥니다. 최소가 화면보다 크면 창을 줄일 수도,
  화면 안으로 넣을 수도 없었습니다.
- **`창 크기 초기화` 단추**를 창 바닥(닫기 옆)에 넣었습니다. 어느 탭에서든 누를 수 있고,
  저장된 창 위치·크기를 지우고 기본 크기로 화면 가운데에 놓으며 월드컵 배율도 100%로
  되돌립니다. 무슨 값이 어긋났든 한 번에 빠져나올 수 있는 자리입니다.

### 💾 기록 내보내기 · 불러오기

아레나 폴더는 하나뿐이라, 다른 주제로 새로 시작하려면 지금까지 쌓은 점수와 그림을 덮어써야
했습니다. 통계 탭에 **`기록 내보내기...`**와 **`기록 불러오기...`**를 넣었습니다.

- 작가 명단 · 조합 · 점수 · 전적과 **그림 파일까지 zip 하나**에 담습니다
  (`arena-20260908-1430.zip`처럼 날짜가 붙은 이름을 제안합니다).
- 폴더가 아니라 zip인 이유는 한 덩어리라 옮기기 쉽고, 그림 수백 장이 압축되며, 반쯤 복사된
  폴더 같은 어중간한 상태가 생기지 않기 때문입니다. 폴더로 풀고 싶으면 탐색기에서 zip을
  그냥 풀면 됩니다.
- 불러오면 지금 기록을 **갈아 끼웁니다.** 무엇이 사라지고 무엇이 들어오는지 개수를 보여 주고
  확인을 받으며, 남기고 싶으면 먼저 내보내라고 안내합니다.
- 남이 건넨 zip일 수 있다고 보고 다룹니다 — 경로가 아레나 폴더 밖을 가리키는 항목은 버리고,
  이미지 확장자가 아닌 파일은 풀지 않습니다.

### 🗑️ 지운 그림은 휴지통으로

아레나가 그림 파일을 지울 때 **OS 휴지통**으로 보냅니다 (Windows 휴지통 · macOS `~/.Trash` ·
Linux freedesktop 규격). 크레딧을 써서 뽑은 그림이라, 앱 안의 `되돌리기`를 놓쳤어도
휴지통에서 되찾을 수 있습니다.

- 대상: 조합 삭제 시 `그림 파일도 휴지통으로`를 켰을 때, 통계 탭의 `안 쓰는 그림 정리`,
  그리고 **프리셋 삭제**. 프리셋도 앱 안에 되돌리기가 없어, 실수로 지웠을 때 되찾을 곳은
  휴지통뿐입니다.
- 네트워크 드라이브처럼 휴지통을 못 쓰는 자리에서는 그냥 지웁니다 — 파일이 남아 버리면
  앱은 지웠다고 여기는데 사용자는 남은 파일을 보게 되기 때문입니다 (로그에 남깁니다).

### ⌨️ 월드컵 판정 단축키

안내에 적힌 `←` `→` `↑` `↓` `Space` `Delete`가 **사실상 동작하지 않았습니다.** 키는 포커스를
가진 위젯으로 가는데, 그림이나 단추를 한 번 누르는 순간 포커스가 거기로 넘어가 방향키는
배율 슬라이더가, `Space`는 마지막에 누른 단추가 먼저 먹었습니다.

- 이제 **어디에 포커스가 있든** 월드컵 탭을 보고 있으면 판정 키가 먼저 받습니다.
- 다른 탭(조합 생성의 숫자 상자 등)과 메인 창은 그대로입니다 — 단축키는 월드컵 탭 안으로만
  범위가 묶여 있습니다.
- **`단축키 사용` 체크**로 통째로 끌 수 있습니다 (기본 켜짐). 끄면 그 키들이 슬라이더·목록 등
  원래 받던 곳으로 돌아가고, 판정은 단추로만 합니다. 설정에 남아 다음 실행에 복원됩니다.

### 🪟 창 다루기

`QDialog`은 제목 표시줄에 **닫기 단추만** 줍니다. 잠깐 떴다 사라지는 대화 상자라면 그 편이
맞지만, 오래 띄워 놓고 크게 보는 창까지 그 기본값에 갇혀 있었습니다 — 테두리를 끌어 크기를
맞추는 것 말고는 방법이 없었습니다.

- **최소화 · 최대화 단추**가 붙었습니다. 최대화가 붙으면서 **제목 표시줄 더블클릭**과
  Windows 스냅(`Win`+`←`/`→`, 화면 가장자리로 끌기)도 함께 동작합니다.
- 적용된 창: **그림체 아레나** · 갤러리 · 이미지 정보 · 프롬프트 어시스턴트 · 로그 뷰어 ·
  마스크 편집기. `exec()`로 뜨는 창(로그 · 마스크)에는 최소화를 넣지 않았습니다 — 부모 창이
  입력을 못 받는 채로 자식만 내려가면 빠져나올 방법이 없어집니다.
- **저장된 위치가 화면 밖이면 창을 안으로 끌어옵니다.** 모니터 구성이 바뀌었거나 값이
  어긋나 제목 표시줄이 화면 위로 넘어가면, 창을 끌 수도 닫을 수도 없었습니다. 화면보다 커진
  창도 화면에 맞춰 줄입니다 (그림체 아레나 · 이미지 정보).
- **전체 화면(`F11`)은 넣지 않았습니다.** 개발 중 잠깐 넣었다가 걷어냈습니다 — Windows에서
  부모가 있는 대화 상자를 전체 화면으로 만들면 **창틀 높이만큼 내용이 위로 밀려** 탭 줄과
  상단 컨트롤이 화면 위로 잘려 나갔습니다. 크게 보는 것은 최대화로 됩니다.

### 📝 참고

- 화면에서 만진 값(작가 수 · 가중치 범위 · 배율 · 리그 · 교배 설정 등)은 전부 설정에 남아
  다음 실행에 그대로 복원됩니다.
- 아레나 창은 모드리스라 메인 창을 막지 않고, 창을 닫아도 진행 중인 생성은 멈추지 않습니다
  (멈추려면 `중지`를 누릅니다).

### 🙏 크레딧

**그림체 아레나**의 아이디어와 평가 방식은 숭이717 님의
[NAI 그림체 이상형 월드컵 v3.0](https://arca.live/b/aiart/181388564)에서 가져왔습니다
([웹 버전](https://calm-nougat-e1db98.netlify.app/)). **원저자의 허락을 받아** 이식했습니다.

코드는 이 저장소에서 새로 작성했고, 위 `점수와 진화` 절에 적은 다섯 곳(시드 · K값 · 대진 ·
작가 점수 분배 · 가중치 유전)은 의도적으로 다르게 잡았습니다. 좋은 아이디어를 공개해 주신
것에 감사드립니다.

---

## v0.7.8 (2026-09-06)

프롬프트 입력이 편해졌습니다. **와일드카드 이름이 자동완성으로 뜨고**, **프롬프트 어시스턴트가
곁창이 되어** 창을 띄운 채로 프롬프트를 고치고 생성을 돌릴 수 있습니다.

### ✨ 새 기능

- **와일드카드 이름 자동완성.** 프롬프트 입력란에 `__`를 치면 와일드카드 폴더 안의 `.txt`
  목록이 그대로 뜹니다. 이어서 글자를 치면 좁혀지고(`__ha` → `__hair__`), 고르면 **닫는
  기호까지 붙습니다.** 공유 랜덤(`__=`)과 루프카드(`##`)도 각각의 기호로 시작하면 같은
  목록이 그 문법으로 나옵니다. 하위 폴더에 넣어 둔 것은 `__style/artist__`처럼 경로째
  나오고, 앱을 켜 둔 채 파일을 새로 넣어도 잠시 뒤부터 목록에 뜹니다.
  - 태그 자동완성과 같은 드롭다운이라 조작도 같습니다 (`↑`·`↓`로 고르고 `Enter`·`Tab`으로
    넣기, `Esc`로 닫기). 프롬프트·네거티브 프롬프트는 물론 캐릭터 슬롯에서도 뜹니다.
  - 쓰던 줄 **도중**에 쳐도 뜹니다 (`1girl, blue hair __ha`). 앞 토큰과의 경계는 쉼표뿐
    아니라 **줄바꿈**과 `|`(캐릭터 구분자)도 됩니다 — 여러 줄로 쓰는 프롬프트에서 앞 줄까지
    한 덩어리로 묶여 태그 자동완성이 뜨지 않던 것도 이참에 고쳤습니다.
  - **태그도 쉼표 없이 이어 써도 뜹니다.** `blue ey` → `blue eyes`처럼 낱말이 여럿인
    태그를 위해 구간 전체로 먼저 찾고, 없으면 **마지막 낱말**로 한 번 더 찾습니다
    (`1girl, solo masterpi` → `masterpiece`). 갈아 끼우는 것도 그 낱말뿐입니다.
  - **스위치는 태그와 따로입니다** — 읽어 오는 곳이 다르기 때문입니다. `옵션 → 자동완성`에
    `태그 자동완성 사용`(태그 DB 파일)과 `와일드카드 자동완성 사용`(와일드카드 폴더)이
    나란히 있습니다. 한쪽을 꺼도 다른 쪽은 그대로 뜨고, 태그 DB를 읽지 못한 상태에서도
    와일드카드 목록은 뜹니다.

### 🔧 개선

- **옵션 화면 정리.** `태그 자동완성` 항목이 **`자동완성`**이 되어 태그·와일드카드 두
  자동완성을 한 자리에 모았고, 거기 있던 **WD14 태거 모델**(모델 폴더 · 태거 모델 ·
  `모델 다운로드`)은 **`폴더 경로`** 항목으로 옮겼습니다 — 모델도 결국 "어느 폴더의 어느
  파일"이라, 결과·와일드카드 같은 다른 경로들과 한 화면에 있는 편이 찾기 쉽습니다.
  설정 값은 그대로이므로 따로 다시 지정할 것은 없습니다.
- **프롬프트 어시스턴트(`Ctrl+G`)가 곁창으로.** 지금까지는 창이 떠 있는 동안 메인 창이
  잠겨서, 어시스턴트를 닫아야 프롬프트를 고치거나 생성을 누를 수 있었습니다. 이제는 띄워
  둔 채로 메인 창을 그대로 씁니다.
  - `프롬프트에 이어붙이기` · `프롬프트 덮어쓰기`를 눌러도 **창이 닫히지 않습니다.** 결과를
    메인 창에 넣어 보고, 마음에 안 들면 그 자리에서 다시 생성해 덮어쓰면 됩니다.
  - 이미 떠 있을 때 `Ctrl+G`를 누르면 새 창을 만들지 않고 그 창을 앞으로 가져옵니다.
  - 메인 창을 닫으면 어시스턴트·갤러리·이미지 정보 창도 함께 닫힙니다 (곁창이 남아 앱이
    끝나지 않던 문제도 함께 정리했습니다).

---

## v0.7.7 (2026-09-06)

메뉴를 정리했습니다. **이미지 정보 열기**가 `파일`이 아니라 `도구` 메뉴로 옮겨 갔고, 메뉴와
라벨에 붙어 있던 **영어 병기를 걷어냈습니다** — 한국어로 쓸 때는 한국어만 보입니다.

### 🔧 개선

- **`이미지 정보 열기`가 `도구` 메뉴로.** 로그 보기·프롬프트 어시스턴트·프리셋과 같은 자리에
  모았습니다. 여는 방법(창 안의 `파일 열기`, 메인 창이나 프롬프트 입력란에 끌어다 놓기)은
  그대로입니다.
- **영어 병기 제거.** `파일(Files)` · `도구(Tools)` · `랜덤 (Random)` · `일반 (Normal)`처럼
  낱말 뒤에 영어를 덧붙여 두던 항목을 한국어·일본어·중국어 세 언어에서 모두 정리했습니다
  (`파일(Files)` → `파일`). 제품명·약어·단축키(`LM Studio` · `LLM` · `i2i` · `F8`)는 뜻이
  달라지므로 그대로 둡니다.

---

## v0.7.6 (2026-09-05)

프롬프트 어시스턴트가 로컬 LLM에게 보내는 **지시문을 JSON 파일로 빼냈습니다.** 지금까지는
코드에 박혀 있어 손댈 수 없었는데, 이제 항목별로 나뉜 파일 하나를 만들어 메모장으로 고치면
됩니다.

### ✨ 새 기능

- **LLM 프롬프트 세트 (JSON).** `옵션 → LLM`의 `프롬프트 세트 (JSON 파일)`에서
  `기본 파일 만들기`를 누르면 지금 쓰는 문안이 그대로 든 파일이 만들어지고,
  `편집기로 열기`로 바로 고칠 수 있습니다. 다음에 어시스턴트 창(`Ctrl+G`)을 열 때부터
  적용됩니다. 파일을 여러 벌 만들어 두고 `찾아보기...`로 골라 쓸 수도 있습니다
  (모델별로 잘 듣는 문구가 다릅니다).

  나뉘어 있는 항목은 이렇습니다 — 스타일별 역할 지시(`styles.natural` /
  `styles.danbooru`), 공통 출력 규약(`common_rules`), 어시스턴트 모드 변형 지시
  (`assistant_instruction`), 출력 길이 3단의 분량 지시(`length_hints`), 그리고 모델에게
  보내는 사용자 메시지 틀(`user_messages`).

  **고칠 항목만 남기고 나머지는 지워도 됩니다** — 적어 둔 항목만 기본값 위에 덮입니다.
  그래서 앱이 올라가며 항목이 늘어도 예전에 만든 파일이 깨지지 않습니다.
  `user_messages`에서는 `{kind}`·`{text}`만 값으로 바뀌고 그 밖의 중괄호는 적은 그대로
  가므로, `{"prompt": "..."}` 같은 JSON 예시를 그대로 써도 됩니다.

  경로를 비워 두면 지금까지와 똑같이 내장 기본 문안을 씁니다. 파일이 깨졌거나 경로가
  틀리면 이유를 알려 주고 **기본 문안으로 계속 진행**합니다 — 파일 하나 때문에 기능이
  막히지 않습니다. 옵션의 `시스템 프롬프트` 칸은 그대로 남아 있고, 조립된 지시문 맨 뒤에
  덧붙습니다.

### 🔧 기타

- 설정 스키마가 4로 올라갑니다 (프롬프트 세트 경로 한 줄이 추가됐습니다). 기존
  설정 파일은 그대로 열리고, 값은 하나도 바뀌지 않습니다.

---

## v0.7.5 (2026-09-04)

화면을 좁은 곳에서도 쓸 수 있게 손본 판입니다. 잘 안 건드리는 영역은 접고, 입력 패널은
기본 폭의 절반까지 줄일 수 있게 했으며, V4에 있던 "생성된 이미지의 프롬프트 표시"를
가져왔습니다. 새 단축키는 `F8`(프롬프트 결과 표시)과 `F9`(입력 패널 좁게) 두 개입니다.

### ✨ 새 기능

- **캐릭터 위치 지정 캔버스를 접을 수 있습니다.** 캐릭터 프롬프트 안의 `위치 지정` 머리글을
  누르면 캔버스가 접히고, 그 자리에 지금 좌표가 `1 C3 · 2 D3`처럼 한 줄 요약으로 남습니다.
  좌표는 접어도 그대로 유지되므로 생성 결과는 달라지지 않습니다. 자리를 한 번 잡고 나면
  200px 넘는 캔버스가 캐릭터 슬롯을 화면 밖으로 밀어내기만 했는데, 그만큼을 슬롯이 씁니다.
  접힘 상태는 종료 시 저장됩니다 (`보기 → 레이아웃 초기화`로 다시 펼칩니다).

- **캔버스 높이도 드래그로 조절합니다.** 캔버스 아래 핸들을 끌어 120~600px 사이에서 키우거나
  줄일 수 있습니다. 마커를 정확한 자리에 놓을 때는 캔버스가 넓은 편이 낫고, 높이도
  세션 간 유지됩니다.

- **생성된 이미지의 프롬프트를 이미지 위에 표시합니다 (V4 기능 이식).** 생성 바의
  `프롬프트 결과 표시` 체크박스로 켭니다 (보기 메뉴 · `F8`도 같은 상태를 가리킵니다).
  결과 이미지 아래쪽 40%에 반투명 판이 얹히고, 방금 만든 그 장의 프롬프트·네거티브·
  캐릭터(좌표 포함)·모델·크기·시드·스텝·Prompt Guidance·Rescale·샘플러·노이즈 스케줄이
  적힙니다. 화면의 입력란이 아니라 **실제로 보낸 값**이라 와일드카드(`__파일__`)·아티스트
  조합·`<a|b|c>` 선택이 전개된 뒤의 프롬프트가 나옵니다 — 연속 생성 중 마음에 드는 그림이
  나왔을 때 어떤 조합이었는지 바로 확인할 수 있습니다. 켬/끔 상태는 종료 시 저장되고,
  켜고 꺼도 이미지 크기는 달라지지 않습니다.

- **캐릭터 슬롯을 하나씩 접을 수 있습니다.** 슬롯 왼쪽 끝 화살표를 누르면 헤더 한 줄로 접히고
  프롬프트 앞 40자가 미리보기로 남습니다. 값은 그대로라 생성 결과는 달라지지 않습니다.
  캐릭터가 서너 명만 돼도 세로가 금방 차는데, 지금 손대지 않는 슬롯만 접어 두면 됩니다.

- **`보기 → 입력 패널 좁게` (`F9`).** 켜면 입력 패널이 최소 폭(230px)으로 접히고, 다시 누르면
  접기 직전 폭으로 돌아갑니다. 아래 항목으로 좁힐 수는 있게 됐지만 매번 스플리터를 끌어야 했습니다.

- **좌측 입력 패널을 기본 폭의 절반(230px)까지 좁힐 수 있습니다.** 전에는 생성 바의 버튼 줄
  때문에 좌측 열의 최소 폭이 618px로 고정돼, 스플리터를 아무리 밀어도 그 아래로 내려가지
  않았습니다. 이제 생성 버튼·퀵 매수 버튼, 해상도 비율 버튼(Wide/Square/Portrait), 캐릭터
  프롬프트 상단 버튼은 한 줄에 다 들어가지 않으면 **다음 줄로 넘어갑니다** — 잘리거나
  사라지지 않습니다. 결과 이미지를 크게 보면서 작업할 때 씁니다.

### 🔧 개선

- **`매 장마다 해상도 랜덤 (세로/가로/정방형)` 체크박스 문구를 `해상도 랜덤`으로 줄였습니다.**
  괄호 안 설명은 원래부터 툴팁에 그대로 있습니다. 이 한 줄이 생성 바에서 가장 넓은 위젯이라,
  패널을 좁힐 때 하한을 혼자 251px로 끌어올리고 있었습니다.

- **접힌 섹션의 요약 한 줄이 패널 폭을 붙잡지 않습니다.** `AI 설정`을 접었을 때 나오는
  `스텝 28 · PG 5 · 시드 랜덤 …` 줄의 길이가 그대로 좌측 패널의 최소 폭이었습니다.

### 🐛 버그 수정

- **`보기 → 레이아웃 초기화`(`Ctrl+R`)가 기본 분할 비율을 실제로 되돌립니다.** 좌측 열의
  최소 폭(618px)이 기본값(460px)보다 커서, 초기화해도 입력 패널이 결과 패널보다 넓게
  남아 있었습니다.

---

## v0.7.4 (2026-09-04)

### ✨ 새 기능

- **이미지 정보를 전용 창에서 다룹니다 (V4와 같은 흐름).** `파일 → 이미지 정보 열기`를
  누르면 파일 선택 창이 먼저 뜨던 것을, 이제 **이미지 정보 창이 먼저** 뜹니다. 창 안에서
  `파일 열기`를 누르거나 이미지를 끌어다 놓으면 되고, 창은 모드리스라 메인 화면을 막지
  않습니다. 파일을 바꿔 가며 여러 장을 훑어볼 때 창이 다시 뜨지 않고 내용만 바뀝니다.

  창 안에서 달라진 점:

  | 항목 | 내용 |
  |---|---|
  | 프롬프트·네거티브·캐릭터 프롬프트 | 잘라 쓴 요약이 아니라 **전문**을 읽기 전용 상자에 |
  | 시드·스텝·Prompt Guidance·샘플러·크기 | 값과 함께 항목별 체크박스로 |
  | `선택 항목 적용` | **체크한 항목만** 되돌려 놓습니다 (전에는 체크와 무관하게 전부 적용) |
  | `선택 항목 복사` | 같은 내용을 클립보드로 |

  메타데이터에 없는 항목은 줄 자체를 감춥니다.

### 🐛 버그 수정

- **웹을 거쳐 다시 압축된 PNG의 생성 정보가 보이지 않던 문제를 고쳤습니다.** 커뮤니티·SNS에
  올렸다 내려받은 PNG는 용량 최적화기를 거치면서 텍스트 청크가 **이미지 데이터(IDAT) 뒤로
  밀리는** 일이 흔한데, Pillow는 그 자리의 청크를 `Image.open()` 시점에는 읽지 않습니다
  (`load()`가 끝나야 들어옵니다). V4의 확인기와 웹 EXIF 도구에서는 보이는데 V5에서만
  "정보 없음"으로 나오던 이유가 이것입니다. 이제 다음 경우를 모두 읽습니다.

  - IDAT 뒤로 밀린 tEXt/zTXt/iTXt 청크
  - zTXt로 압축된 텍스트 청크
  - 이름의 대소문자가 바뀐 필드 (`comment`, `COMMENT`)
  - 텍스트 청크 대신 **EXIF**(eXIf 청크의 `UserComment` / `ImageDescription`)로 옮겨 간 경우
  - 텍스트가 통째로 털리고 **숨김 메타데이터(알파 채널 LSB)만 남은 경우** — 웹을 거친
    이미지에서 가장 흔한 모습입니다. NovelAI의 이미지 확인 페이지가 읽는 것도 이쪽입니다

  알려진 필드가 하나도 없더라도 남아 있는 텍스트는 창의 `원본 메타데이터`에 그대로 보여 줍니다.

- **WebP 이미지의 숨김 메타데이터를 읽지 못하던 문제를 고쳤습니다.** 숨김 메타데이터는
  알파 채널의 최하위 비트에 실려 있어 **무손실이면 WebP에서도 그대로 남는데**, 형식이
  PNG가 아니라는 이유로 아예 확인하지 않았습니다. 정보가 파일 안에 멀쩡히 있는데도
  "생성 정보가 없습니다"로 보이던 경우가 여기에 해당합니다. 이제 V4와 같이 형식을
  가리지 않고 확인합니다.

  **게시판에서 받은 이미지가 특히 이렇습니다.** 용량을 줄이려고 WebP로 변환하면서
  파일명은 `.png` 그대로 두는 곳이 많아, 겉보기에는 PNG인데 내용은 WebP입니다. 이런
  파일도 이제 프롬프트·시드·캐릭터까지 전부 복원됩니다.

- **PNG-8(팔레트)로 재양자화된 이미지를 열면 정보 읽기가 통째로 실패하던 문제를
  고쳤습니다.** 숨김 메타데이터(stealth) 리더가 팔레트/그레이스케일 픽셀을 RGB 튜플로
  받으려다 예외를 냈습니다. 이제 이런 형식은 건너뜁니다 — 어차피 색이 재양자화되면 숨은
  비트는 남지 않습니다.

- **프롬프트 입력란에 이미지를 끌어다 놓으면 파일 경로가 글자로 붙던 문제를 고쳤습니다.**
  V4처럼 이미지 정보 창이 뜹니다. 메인 프롬프트와 캐릭터 슬롯 입력란 모두 같습니다.
  이미지가 아닌 드롭(텍스트 등)은 예전 그대로 붙습니다.

- **갤러리에서 썸네일을 눌러 고른 설정이 곧바로 적용됩니다.** 전에는 갤러리 창의 정보
  대화상자에서 `적용`을 눌러도 같은 창이 한 번 더 뜨고, 거기서 다시 눌러야 했습니다.

---

## v0.7.3 (2026-09-02)

### ✨ 새 기능

- **동일 조건으로 다시 생성할 때의 동작을 고를 수 있습니다 (옵션 → 생성).** 이미지를 끌어다
  놓거나 이미지 정보에서 설정을 불러오면 시드까지 복원되므로, 생성을 두 번 누르면 완전히 같은
  요청이 두 번 나가 같은 이미지에 크레딧이 두 번 나갔습니다.

  | 선택 | 동작 |
  |---|---|
  | **시드를 자동으로 랜덤 변경** (기본) | 시드만 새로 뽑아 생성하고, 바뀐 시드를 시드 입력란과 상태바에 표시합니다 |
  | **그대로 생성** | 예전과 같습니다 (같은 이미지가 한 장 더) |
  | **경고하고 생성하지 않음** | 경고 후 멈춥니다 — 시드를 바꾸거나 `랜덤`을 켜면 다시 생성됩니다 |

  판정은 **직전에 완성한 이미지**의 요청과 프롬프트·시드·해상도·스텝·CFG·샘플러·캐릭터
  프롬프트·i2i 원본까지 전부 같은 경우입니다. 시드가 `랜덤`이거나, 프롬프트에 와일드카드
  (`__name__`)·꺾쇠 랜덤(`<a|b>`)·아티스트 조합(`{artist:그룹}`)·NovelAI Prompt Randomizer
  (`||a|b||`)가 있거나, 생성이 실패해 이미지가 나오지 않은 경우에는 개입하지 않습니다
  (같은 값으로 재시도할 수 있어야 하므로).

### 🐛 버그 수정

- **창 크기·패널 폭·프롬프트 높이가 다시 켜면 초기화되던 문제를 고쳤습니다.** 프롬프트/캐릭터
  프롬프트 섹션의 가로 폭(좌·우 스플리터), 창 크기와 위치, 프롬프트·캐릭터 슬롯 입력란 높이,
  이미지 정보 창의 분할 비율은 모두 `QSettings`에 저장되는데, 앱이 **조직 이름을 지정하지
  않아** Windows 레지스트리 백엔드가 `AccessError` 상태로 열리고 있었습니다. 이 상태에서는
  읽기도 쓰기도 조용히 무시되므로(예외도, 로그도 없습니다) 저장한 적이 없는 것과 같았습니다.
  이제 `sagawa8b / NAI-Auto-V5`로 지정해 `HKCU\Software\sagawa8b\NAI-Auto-V5`에 저장합니다.

  덧붙여 종료 시 저장은 이벤트 루프가 끝난 뒤에 일어나 Qt의 주기적 flush를 탈 수 없으므로,
  `QSettings.sync()`로 직접 내려쓰도록 했습니다.

  > 이번 버전에서 UI 상태 저장이 처음으로 실제 동작합니다 — 한 번 더 창 크기와 폭을 맞춰
  > 두면 그 다음 실행부터 그대로 복원됩니다.

---

## v0.7.2 (2026-09-02)

### 🐛 버그 수정 / 문법 변경

- **맨 `|`를 NovelAI 멀티 캐릭터 문법으로 되돌렸습니다 (이슈 #4).** v0.6.0에서 "쉼표 구간의
  `a|b|c`를 앱이 랜덤으로 하나 고른다"는 문법을 넣었는데, 이는 *NovelAI의 `|`는 Prompt
  Mixing이다*라는 잘못된 전제 위에 만들어진 것이었습니다. Prompt Mixing은 V3 문법이고 V4
  이후로는 없습니다 — V4/V5에서 맨 `|`는 **베이스 프롬프트와 캐릭터 프롬프트를 나누는
  구분자**이고(`base | character 1 | character 2`), 공식 랜덤은 `||a|b|c||`입니다
  ([멀티 캐릭터](https://docs.novelai.net/en/image/multiplecharacters/) /
  [Prompt Randomizer](https://docs.novelai.net/en/image/promptrandomizer/) 문서).

  그래서 v0.6.0~v0.7.1에서는 두 공식 문법이 모두 깨져 있었습니다.

  | 입력 | v0.7.1까지 | v0.7.2 |
  |---|---|---|
  | `2girls, outdoors \| girl, blonde hair \| girl, black hair` | `2girls, outdoors, blonde hair, black hair` (토큰 유실) | 베이스 + 캐릭터 프롬프트 2개 |
  | `1girl, \|\|red hair\|blue hair\|\|, rain` | 2/3 확률로 `1girl, , rain` (빈 값) | 그대로 NovelAI로 전달 |

- **프롬프트에 쓴 `|`로 캐릭터 프롬프트를 만듭니다.** 캐릭터 프롬프트 칸이 **모두 비어 있을
  때만** 메인 프롬프트를 `|`로 나눠 첫 조각을 베이스로, 나머지를 캐릭터 프롬프트로 보냅니다
  (칸이 하나라도 채워져 있으면 파이프 문법을 쓰지 않는 NovelAI 웹UI 규칙과 같습니다).
  이렇게 만든 캐릭터는 좌표를 넘기지 않습니다 — 배치는 AI가 정합니다. 와일드카드가 만들어 낸
  `|`도 잡히므로, 와일드카드 한 줄로 캐릭터 구성 전체를 바꿀 수 있습니다.

- **랜덤 선택 문법은 꺾쇠 `<a|b|c>`만 남습니다.** 꺾쇠는 NovelAI 문법에 없어 충돌하지 않습니다.
  기존에 `1girl, red|blue hair`처럼 쓰던 프롬프트·와일드카드는 `1girl, <red|blue> hair`로
  바꿔 주세요. 공식 `||red|blue||`를 그대로 써도 됩니다(NovelAI가 처리합니다).

---

## v0.7.1 (2026-09-02)

### ✨ 새 기능 / 변경

- **NAI 프롬프트 어시스턴트로 통합.** `WD14 자동 태깅`과 `자연어 프롬프트 생성` 두 메뉴를
  하나로 합쳤습니다 — 도구 → `NAI 프롬프트 어시스턴트`(`Ctrl+G`). 한 창에서 모드만 바꿔
  세 가지를 씁니다

  | 모드 | 하는 일 |
  |---|---|
  | WD 태거 | 로컬 WD14 모델로 이미지 → 단부루 태그 (기존 기능) |
  | LLM 태거 | LM Studio 모델로 텍스트·이미지 → 자연어 프롬프트 또는 단부루 태그 |
  | LLM 어시스턴트 | 이미지 + 텍스트 **지시**로 원하는 변형 결과 생성 |

  - **입력이 좌우로 나뉩니다** — 왼쪽 텍스트, 오른쪽 이미지. 값이 있는 쪽만 쓰고 빈 쪽은
    무시합니다 (탭 전환이 없어졌습니다)
  - **LLM 어시스턴트**는 이미지를 텍스트 지시대로 바꿔 프롬프트로 냅니다. 예: 기모노 입은
    여성 이미지 + "기모노를 한복으로" → 한복으로 바뀐 프롬프트, 왼쪽으로 활 쏘는 이미지 +
    "활을 오른쪽으로 쏘는 구도" → 구도가 바뀐 프롬프트. 자연어/단부루 중 형태를 고를 수
    있습니다
  - WD 태거 결과는 이제 **편집 가능한 텍스트**로 나옵니다 (체크박스 목록 대신) — 바로
    고쳐서 넣거나 복사할 수 있습니다
  - **출력 길이(짧게/중간/길게)**를 고를 수 있습니다 (출력 스타일과 생성 버튼 사이). 모델에
    따라 자연어가 지나치게 길게 나오는 것을 조절합니다. LLM은 프롬프트에 분량 지시(대략적
    글자 수)를 주는 방식이라 문장이 중간에 끊기지 않고, WD 태거는 일반 태그 임계값(낮을수록
    태그가 많아짐)으로 반영됩니다
  - **추론(think) 모델의 사고 과정이 결과에 섞여 나오던 문제를 고쳤습니다** — 실제 답변만
    추려서 넣습니다
  - 모드에 따라 필요 없는 항목(모델·출력 스타일 등)은 자동으로 잠깁니다

### 🔧 정리

- 기존 `WD14 자동 태깅`·`자연어 프롬프트 생성` 창은 통합 창으로 대체되었습니다 (단축키
  `Ctrl+G`로 통일).

---

## v0.7.0 (2026-09-02)

### ✨ 새 기능

- **자연어 프롬프트 생성 (LM Studio 연동).** 도구 → `자연어 프롬프트 생성`(`Ctrl+G`)으로
  창을 엽니다. PC에서 돌아가는 [LM Studio](https://lmstudio.ai/)의 로컬 LLM에 붙어, 입력한
  단어·문장·이미지를 NovelAI V5용 프롬프트로 만들어 줍니다. V5는 자연어 이해력이 좋아진
  모델이라, 서술형 문장을 그대로 프롬프트로 쓸 수 있습니다

  - **텍스트 / 이미지 두 가지 입력.** 텍스트 탭에 단어·문장을 넣거나, 이미지 탭에 그림을
    끌어다 놓습니다. 이미지 분석은 **비전 모델(VLM, 예: Qwen VL·Gemma 비전)**에서 됩니다.
    생성할 때는 **현재 열려 있는 탭의 입력만** 씁니다 — 다른 탭에 남은 값이 섞이지 않습니다
  - **출력 스타일 두 가지.** `자연어 프롬프트`(서술형 문장)와 `단부루 태그`(`1girl, solo, ...`)를
    라디오로 고릅니다. 스타일마다 LLM 지시를 다르게 줘서, 같은 입력에도 형태가 오락가락하지
    않습니다
  - **모델은 LM Studio에서 로드해 둔 것 중에서** 창의 목록으로 고릅니다. 앱이 모델을
    저장하지 않으므로, 앱 때문에 서버에 여러 모델이 로드되는 일이 없습니다
  - **중지 버튼.** 생성 도중 멈출 수 있습니다
  - 결과는 편집한 뒤 `프롬프트에 이어붙이기` / `프롬프트 덮어쓰기` / `복사`로 반영합니다.
    LLM이 네거티브를 제안하면 네거티브 칸에도 함께 넣습니다
  - 서버 주소·응답 대기 시간·기본 스타일·기본 반영 방식은 옵션 → `프롬프트 생성(LLM)`에서
    설정합니다. 시스템 프롬프트에 추가 지시를 넣어 스타일을 더 강하게 굳힐 수도 있습니다
  - `lmstudio` 파이썬 SDK는 배포본(exe)에 함께 들어 있습니다. 소스로 실행 중이면
    `pip install lmstudio`로 설치하세요

  > 로컬 LLM은 사용자 PC에서 도는 LM Studio가 서빙합니다 — NAI-Auto-V5가 모델이나 서버를
  > 포함하지 않습니다. LM Studio를 켜고 모델을 로드해 두어야 씁니다.

---

## v0.6.9 (2026-09-01)

### ✨ 새 기능

- **프롬프트로 화면 비율 고르기.** 메인 프롬프트나 와일드카드 항목에 `<res:portrait>`,
  `<res:square>`, `<res:wide>`를 쓰면 그 장은 현재 해상도 등급의 세로형·정방형·가로형
  해상도로 나옵니다. 프롬프트 내용과 비율을 묶어 둘 수 있어서, 등급 안에서 아무거나 고르는
  `매 장마다 해상도 랜덤`과는 쓰임이 다릅니다

  ```
  1girl, full body, standing, <res:portrait>
  ```

  - 지시어는 NovelAI로 나가기 전에 프롬프트에서 지웁니다 (빈 쉼표 구간도 정리합니다)
  - 대소문자를 가리지 않고, 여러 개 있으면 마지막 것을 씁니다
  - `매 장마다 해상도 랜덤`보다 우선합니다. 지금 등급에 그 비율이 없으면(Wallpaper에는
    정방형이 없습니다) 랜덤으로 되돌리지 않고 현재 해상도를 그대로 씁니다
  - i2i·인페인팅·폴더 강화처럼 크기가 원본 이미지로 정해지는 경우에는 해상도를 바꾸지
    않습니다 (지시어를 지우기는 합니다)
  - `<res:banana>`처럼 모르는 값은 지시어가 아니므로 프롬프트에 그대로 남습니다

  > 공개 저장소에 처음 들어온 기여입니다 — 원안과 초안 구현은
  > [aliceknowing](https://github.com/aliceknowing) 님
  > ([PR #1](https://github.com/sagawa8b/NAI-Auto-Generator-V5/pull/1)).

---

## v0.6.8 (2026-08-31)

### ✨ 새 기능

- **강화 업스케일 (Enhance).** 보기 → `강화 업스케일`(`F4`)로 패널을 켭니다. 이미 만든
  그림을 다시 그려 크게 만드는 기능으로, NovelAI 웹 UI와 같은 세 배율을 그대로 씁니다.
  V4.5의 Enhance와는 **동작이 다릅니다** — V5에서 `Max` 배율이 새로 생겼고, 1.5x와 Max는
  서로 다른 방식으로 큰 그림을 만듭니다

  | 배율 | 하는 일 |
  |---|---|
  | 1x | 크기는 그대로, 디테일만 다시 그립니다 |
  | 1.5x | 강화 해상도로 **먼저 키운 뒤** 그 해상도에서 다시 그립니다 (832×1216 → 1280×1856). 확대 티를 지우려고 프롬프트 끝에 `-2::upscaled, blurry::,`를 자동으로 붙입니다 |
  | Max | 원본 해상도에서 디테일만 다시 그리고, **서버가** 결과를 최대 크기까지 키웁니다 (832×1216 → 1467×2144, 1024×1024 → 1773×1773) |

  원본 PNG에 남은 프롬프트·샘플러·스텝을 그대로 가져와 강화합니다(시드는 새로 뽑습니다 —
  웹 UI도 그렇습니다). 메타데이터가 없는 이미지도 화면의 프롬프트로 강화할 수 있습니다
  (V4.5는 메타데이터가 없으면 거부했습니다)

- **고를 수 없는 배율은 잠기고, 왜인지 알려 줍니다.** 강화 결과에는 약 3.1MP 상한이
  있습니다. Large·Wallpaper 해상도는 1.5x가 이 상한을 넘어서 쓸 수 없고(NovelAI 웹 UI도
  같은 크기에서 1.5x를 감춥니다), 사용자 정의 해상도는 NovelAI가 어떤 강화 해상도를
  쓰는지 알 수 없어 잠급니다 — 짐작한 크기로 Anlas를 쓰는 것보다 낫다고 봤습니다.
  1.5x를 쓸 수 있는 표준 해상도(1024×1024, 832×1216, 1216×832, 640×640, 512×768,
  768×512)를 함께 안내합니다. **Max는 어떤 크기에서도 쓸 수 있습니다** — 가로세로비를
  지켜 상한까지 키우는 계산이라 표에 의존하지 않습니다

- **`최근 결과` 버튼.** 방금 생성한 그림을 곧바로 강화 원본으로 가져옵니다

- **폴더 통째로 강화.** `폴더 선택...`으로 폴더를 고르면 그 안의 이미지를 훑어 대기열을
  만들고, `폴더 강화 시작`을 누르면 **이미지마다 자기 메타데이터의 프롬프트와 자기 크기로**
  한 장씩 강화합니다. 생성 간격과 중지 버튼은 일반 연속 생성과 똑같이 동작합니다.
  시작 전에 대기열을 확인해서, 고른 배율로 처리할 수 없는 파일이 섞여 있으면 **한 장도
  보내지 않고** 몇 장인지 알려 줍니다 (Anlas를 쓰다 중간에 멈추지 않도록)

  > **Anlas 주의** — 강화도 일반 생성과 같은 비용이 듭니다. 실측 1건으로 1024×1024를
  > Max로 강화하니 **37 Anlas**가 나갔습니다. 폴더 강화는 장수만큼 그대로 나가니
  > 대기열 수를 확인하고 시작하세요

- **스모크 CLI로 강화를 실토큰 확인할 수 있습니다.**
  `python -m naiauto.smoke --image <원본.png> --enhance max` — 보내기 전에 계획을 보여
  주고, 결과 PNG 크기와 메타데이터의 `upscaled_*` 값을 앱의 예측과 대조해 알려 줍니다
  (`--dry-run`을 붙이면 네트워크 없이 payload만 확인합니다)

### 🔍 어떻게 맞췄나

배율별 동작은 웹 UI로 만든 결과 PNG의 **메타데이터 7건**과 **웹 UI 요청 캡처 2건**
(1.5x·Max)을 근거로 맞췄습니다. 우리 요청은 웹 UI가 보내는 것과 **키 단위로 일치**합니다.

**실토큰으로 검증했습니다** — 1024×1024를 Max로 강화하니 결과가 1773×1773으로 앱이 미리
보여 준 예측과 정확히 같았고, 1.5x도 1536×1536으로 정상 생성됐습니다. 근거와 미확인
항목은 `spec/v5/V5_SPEC.md`의 "Enhance" 절에 정리해 두었습니다.

### 🔧 개선

- **i2i·인페인팅 요청이 웹 UI와 더 가까워졌습니다.** `extra_noise_seed`를 웹 UI와 같이
  `시드 - 1`로 보냅니다 (캡처 3건이 모두 그랬는데, 규칙을 확정하지 못해 그동안 시드를
  그대로 보내고 있었습니다). 품질 태그를 끄면 `qualityPresetId`도 `none`으로 보냅니다 —
  지금까지는 태그를 붙이지 않으면서 프리셋만 `standard`라고 알리고 있었습니다

### 🐛 버그 수정

- **원본 설정을 물려받을 때 UC/품질 프리셋 식별자가 남아 있던 문제.** 강화는 원본 PNG의
  프롬프트를 그대로 쓰는데, 그 안에 프리셋 문구가 이미 들어 있는데도 `ucPresetId`를
  `heavy`라고 함께 알리고 있었습니다 (NovelAI 웹 UI는 이럴 때 `none`을 보냅니다).
  폴더 강화와 스모크 CLI에 영향이 있었고, 앱 화면으로 강화할 때는 원래 정상이었습니다
- **NovelAI 메타데이터가 없는 PNG를 읽을 때 앱이 몇십 초씩 멈추던 문제.** 숨은
  메타데이터(stealth PNG info) 리더가 서명이 어긋난 뒤에도 그림의 **모든 픽셀**을 훑고
  있었습니다 — 1024×1536 한 장에 수십 초가 걸립니다. 이제 서명이 들어갈 자리(첫 40픽셀)만
  보고 끝냅니다. 갤러리·이미지 정보·강화 폴더 훑기가 모두 빨라집니다

---

## v0.6.7 (2026-08-31)

### ✨ 새 기능

- **API 계정 전환 (최대 4개).** `파일` → `API 계정 관리`(`Ctrl+Shift+I`)에서 `pst-`
  영구 API 토큰을 4개까지 등록해 두고 골라 쓸 수 있습니다. `저장 및 전환`을 누르면
  그 토큰으로 실제 로그인해 본 뒤 성공했을 때만 전환하며(실패하면 쓰던 계정을 그대로
  유지합니다), 이후 생성 요청부터 새 계정으로 나갑니다. 토큰은 로그인 창과 마찬가지로
  설정 파일이 아니라 OS 키링(Windows 자격 증명 관리자 등)에만 저장됩니다
- **WD14 자동 태깅을 앱 안에서 설정합니다 (V4와 같은 방식).** 옵션 → 태그에
  `WD14 태거 모델` 구역이 생겼습니다 — 모델 폴더를 지정하고, 태거 모델을 고르고,
  `모델 다운로드`로 HuggingFace에서 바로 받습니다(진행률·취소 가능, v3 모델 약 400MB).
  지금까지는 데이터 폴더의 `wd14/`에 `model.onnx` · `selected_tags.csv`라는 이름으로
  직접 넣는 방법밖에 없었습니다. 이미 받아 둔 폴더를 그대로 가리켜도 되고, 파일 이름이
  `<모델 이름>.onnx` + 같은 이름의 `.csv`(V4에서 받아 두는 형태)여도 인식합니다
- **WD14 태그를 클립보드로 복사.** 태깅 창에 `클립보드에 복사` 버튼이 생겼습니다.
  체크한 태그를 프롬프트에 넣을 때와 같은 형태(쉼표 구분)로 복사하며, 창은 닫히지
  않아 복사한 뒤 프롬프트에도 넣거나 선택을 바꿔 다시 복사할 수 있습니다
- **세팅별 연속 생성의 무작위 순서.** 옵션 → 생성에 `세팅별 연속 생성에서 세팅 파일을
  무작위 순서로`가 추가되었습니다. 켜면 고른 세팅 파일을 순서대로 돌지 않고 매 장마다
  무작위로 고릅니다 (같은 파일이 연달아 나오지는 않습니다). 끄면 지금까지와 같이
  고른 순서대로 순환합니다

### 🐛 버그 수정

- **배포본에서 WD14 자동 태깅이 아예 동작하지 않던 문제.** onnxruntime과 numpy가
  선택 의존성이라 exe로 받은 사용자는 모델을 넣어도 "모델을 쓸 수 없음"만 봤습니다.
  이제 두 패키지를 앱에 함께 넣고, 릴리스 전에 `--selftest`가 확인합니다
- **WD14 태그 결과가 엉뚱하게 나오던 문제.** 이미지를 0–1로 정규화한 RGB로 넣고
  있었지만, WD 태거는 **흰색 정사각 패딩 + BGR + 0–255**를 받습니다 (V4에서 쓰던
  절차). 입력 크기도 모델이 알려 주는 값을 쓰고, 등급(rating) 태그는 빼고 캐릭터
  태그에는 별도 기준치(0.85)를 적용하며 밑줄은 공백으로 바꿔 프롬프트에 넣습니다
- **WD14 창이 영어로만 뜨던 문제.** 창의 모든 문구를 4개 언어로 번역했습니다
- **"모델을 쓸 수 없습니다"가 이유를 알려 주지 않던 문제.** 모델이 없는 것과
  onnxruntime을 못 쓰는 것은 할 일이 전혀 다른데 같은 문구가 떴습니다. 이제 각각을
  구분해 알리고(없는 모델 경로 / 실제 import 오류와 설치 방법), onnxruntime을 못 쓰면
  아무것도 못 하는 창을 띄우는 대신 도구 메뉴에서 바로 안내합니다. 옵션 → 태그의 모델
  구역에서도 같은 경고를 미리 보여 줍니다

## v0.6.5 (2026-08-28)

### ✨ 새 기능

- **세팅별 연속 생성 (V4 기능 이식).** 생성 바에 "세팅별 연속 생성" 버튼이
  추가되었습니다. 세팅 파일을 2개 이상 고르면, 생성마다 다음 파일을 자동으로
  불러와 적용하며 순서대로 순환합니다(마지막 다음엔 다시 처음부터). 각 세팅
  파일로 만든 이미지는 `결과 폴더/세팅파일명/`에 따로 저장됩니다. 매수는
  생성 바의 매수 입력란을 그대로 씁니다(0 = 무제한, 중지 버튼으로 종료)
- **연속 생성 중 프롬프트/캐릭터 프롬프트 수정이 다음 이미지부터 반영.**
  해상도와 마찬가지로, 배치가 도는 동안 프롬프트·네거티브·캐릭터 프롬프트를
  고치면 진행 중인 이미지는 그대로 두고 다음 이미지부터 새 값이 적용됩니다
- **좌우 메인 스플리터 폭 기억.** 프롬프트 영역과 결과 패널 사이의 폭 비율이
  이제 세션 간 유지됩니다 — 지금까지는 재실행할 때마다 기본값으로 되돌아갔습니다
- **매뉴얼 추가.** V4의 `MANUAL_KR.md`를 바탕으로 V5에 실제로 있는 기능만 골라
  `MANUAL_KR.md`를 새로 작성했습니다

### 🐛 버그 수정

- **작업 표시줄 아이콘 누락 수정.** 실행 중 창/작업 표시줄에 앱 아이콘 대신
  파이썬 기본 아이콘이 뜨던 문제를 고쳤습니다 (`app.setWindowIcon` +
  Windows `AppUserModelID` 설정, V4와 동일한 방식)

## v0.6.4 (2026-08-26)

### ✨ 새 기능

- **태그 자동완성 on/off.** 옵션 → 태그 자동완성 페이지에 사용 여부 체크박스가
  추가되었습니다. 끄면 태그 DB가 있어도 입력할 때 드롭다운이 뜨지 않습니다
- **창 크기·위치 기억.** 메인 윈도우 크기와 위치가 종료 시 저장되어 다음 실행에
  그대로 복원됩니다 (프롬프트·캐릭터 프롬프트 영역 비율처럼 세션 간 유지)
- **프롬프트 입력란 폰트 크기·색상 지정 (V4 기능 이식).** 옵션 → 인터페이스에서
  프롬프트·네거티브·캐릭터 프롬프트 입력란의 폰트 크기와 기본 글자색을 지정할 수
  있습니다. 추가로 가중치 강조(`2::프롬프트::`)와 감소(`-2::프롬프트::`) 구문의
  강조색·감소색도 V4처럼 각각 따로 지정할 수 있습니다. 모두 기본값으로 되돌리는
  버튼이 있습니다
- **연속 생성 중 해상도 변경이 다음 이미지부터 반영.** 지금까지는 연속 생성을
  시작한 시점의 해상도로 끝까지 고정됐습니다. 이제 생성 중에 해상도 패널에서
  값을 바꾸면(직접 입력, Wide/Square/Portrait 버튼, 등급 변경 모두) 진행 중인
  이미지는 그대로 두고 다음 이미지부터 새 값이 적용됩니다. "매 장마다 해상도
  랜덤"이 켜져 있으면 후보 목록도 함께 갱신됩니다

## v0.6.0 (2026-08-26)

### ✨ 새 기능

- **프롬프트 랜덤 선택 문법 (`<a|b|c>`, `a|b`).** V4에서 지원하던 랜덤 선택 문법을
  이식했습니다. `<aaa|bbb|ccc>`처럼 꺾쇠로 묶거나, 쉼표로 나눈 구간에 그냥
  `aaa|bbb`라고 쓰면 매 생성마다 그중 하나를 무작위로 골라 보냅니다. NovelAI 자체
  `|` 문법(두 프롬프트를 섞는 Prompt Mixing)과는 별개로, 이 문법은 서버로 보내기
  전에 클라이언트에서 먼저 하나를 골라 치환합니다
- **연속 생성 시 해상도 랜덤 선택.** 생성 바에 "매 장마다 해상도 랜덤 (세로/가로/
  정방형)" 체크박스가 추가되었습니다. 켜면 매 이미지마다 현재 등급(일반/Large/
  Wallpaper/커스텀)에서 사용 가능한 Wide·Square·Portrait 중 하나를 무작위로 골라
  생성합니다. i2i·인페인팅으로 해상도가 원본 이미지에 고정된 동안에는 적용되지
  않습니다

### 🐛 버그 수정

- **프리셋 저장/불러오기 시 캐릭터 프롬프트가 사라지던 문제 수정.** 프리셋
  매니저와 설정 파일 저장(Ctrl+S)/불러오기(Ctrl+O) 모두에서 캐릭터 프롬프트,
  위치 좌표, AI 위치 선택/수동 위치 지정 상태가 저장되지 않고 있었습니다. 이제
  다른 생성 파라미터와 함께 그대로 저장·복원됩니다

## v0.5.0 (2026-08-25)

### ✨ 새 기능

- **프롬프트 입력 영역 리사이즈 가능.** 프롬프트 탭 아래의 드래그 핸들을 잡고 끌면
  편집 영역의 높이를 120~600px 범위에서 자유롭게 조절할 수 있습니다. 높이는 세션 간
  유지됩니다
- **캐릭터 프롬프트 슬롯별 리사이즈.** 각 캐릭터 슬롯의 프롬프트 영역도 동일하게
  드래그로 높이 조절 가능합니다. 한 슬롯을 조절하면 모든 슬롯이 같은 높이로 동기화됩니다
- **프롬프트 ↔ 캐릭터 프롬프트 섹션 비율 조절.** 두 영역 사이의 구분선(스플리터)을
  드래그하여 비율을 자유롭게 조절할 수 있습니다 (V4와 동일). 비율은 세션 간 유지됩니다
- **캐릭터 1명일 때 수동 위치 선택 기능.** "수동 위치 지정" 토글을 켜면 캐릭터가
  1명일 때도 PositionPicker에서 자유 위치를 지정할 수 있습니다 (WebUI와 동일 사양)
- **이미지 정보 분석기 독립 창 + 드래그 앤 드롭.** V4처럼 별도의 모달리스 창으로
  이미지를 끌어다 놓으면 메타데이터를 즉시 확인할 수 있습니다. 스플리터 레이아웃
  (좌: 메타데이터, 우: 줌 가능 이미지 미리보기), 필드별 체크박스 선택 적용을 지원합니다
- **이미지 정보 및 드롭에 WebP 지원.** 파일 열기 다이얼로그와 드래그 앤 드롭 모두
  `.webp` 파일을 처리합니다
- **연속 생성 딜레이 카운트다운.** 연속 생성 대기 시 상태표시줄에 매 초 남은 시간이
  갱신됩니다 (5→4→3→2→1→생성중, V4와 동일)

### 🔧 개선

- ResizeHandle 위젯이 설정 키와 기본 높이를 매개변수로 받을 수 있도록 확장
- DialogImageView: 이미지 정보 대화상자 전용 줌 범위 (10%~500%) + 더블클릭 리셋
- PNG/WebP 메타데이터 동등성 프로퍼티 테스트 추가
- 숫자 필드 JSON 라운드트립 정밀도 프로퍼티 테스트 추가

## v0.4.2 (2026-08-24)

### 🐛 버그 수정

- **결과 미리보기가 V4보다 흐려 보이던 문제 수정.** 화면 배율이 100%가 아닌 환경
  (윈도우 125%·150% 등)에서 미리보기가 눈에 띄게 뭉개졌습니다. Qt6은 고DPI 스케일링을
  끌 수 없는데(V4의 Qt5는 꺼져 있어 1:1이었습니다) 이미지를 **논리 픽셀** 크기로만
  줄여서 넘기고 있었습니다. 화면에 그릴 때 배율만큼 다시 확대되니, 원본에 그 픽셀이
  남아 있는데도 버리고 늘린 셈이었습니다. 이제 화면이 실제로 찍을 픽셀 수만큼
  축소해 넘깁니다 — 150% 배율 기준으로 **약 1.4배 또렷**해집니다
  (배율 100%에서는 이전과 동일합니다)
- **갤러리 썸네일 · i2i 썸네일 · WD14 창 미리보기**도 같은 이유로 흐렸습니다.
  네 곳 모두 같은 방식으로 고쳤습니다

> 참고로 원인은 두 겹이었습니다. 배율만큼 큰 이미지를 만들어도 `QLabel`이 그것을 다시
> 논리 크기로 줄여 버려(1픽셀 체커보드가 균일한 회색이 됩니다) 소용이 없었습니다.
> 그래서 미리보기는 이미지를 직접 그리도록 바꿨습니다.

## v0.4.1 (2026-08-24)

### 🐛 버그 수정

- **인페인팅 마스크 경계에 이음선이 보이던 문제 수정.** v0.4.0에서 `마스크 밖 원본 유지`를
  기본으로 켜 두었는데, NAI 웹UI 요청을 실제로 받아 대조해 보니 **웹UI는 이 값을 켜지
  않습니다**(캡처 2건 모두). 켜 두면 마스크 밖은 확실히 보존되지만 생성된 안쪽과 만나는
  경계가 드러납니다. 기본을 웹UI와 같게 **끄기로 되돌렸습니다** — 체크박스는 그대로
  남아 있어 필요할 때 켤 수 있습니다
- **마스크를 잠재 격자에 맞춰 보내도록 수정.** 확산 모델은 1/8 해상도에서 동작하는데
  우리는 전해상도 마스크를 그대로 보내고 있었습니다. 경계가 격자에 걸치면 절반만 덮인
  칸이 생겨 경계가 지저분해집니다. 이제 NAI 웹UI와 같은 8×8 블록 단위로 맞춥니다
  (칠한 영역이 줄어드는 일은 없습니다)
- **변화 강도를 1.00까지 올릴 수 있게 했습니다.** NAI 웹UI의 Strength 슬라이더는
  0.01~1.00인데 우리는 0.99에서 막혀 있어 웹UI와 같은 값을 낼 수 없었습니다

> 마스크 밖이 변해서 곤란하시면 강도를 **0.7 안팎**으로 두세요. NAI 웹UI도 0.61~0.7을
> 씁니다. 강도가 0.9를 넘으면 이미지 전체를 거의 새로 뽑기 때문에 마스크 밖까지 흔들립니다.

### 🔍 검증

- 웹UI 인페인팅 요청 캡처를 받아 우리 페이로드와 키 단위로 대조했습니다 —
  **빠진 키·남는 키·값이 다른 키가 모두 0**(41개 전부 일치). 이 대조를 골든 테스트로
  고정해 두었습니다

---

## v0.4.0 (2026-08-24)

### ✨ 신규 기능

**마스크 밖 원본 유지** — 인페인팅에서 칠하지 않은 영역이 바뀌지 않게 합니다.

- i2i 패널에 `마스크 밖 원본 유지` 체크박스를 추가했습니다 (**기본 켜짐**).
  NAI 웹UI의 `Overlay Original Image`에 해당합니다
- 지금까지는 이 값이 꺼진 채로 고정돼 있어, 인페인팅을 하면 이미지 전체가 다시 생성되면서
  **칠하지 않은 영역까지 미세하게 바뀌었습니다**(노이즈처럼 보이던 증상). 이제 칠한 곳만
  바뀝니다
- 마스크 경계를 더 자연스럽게 섞고 싶을 때는 꺼서 예전 동작을 쓸 수 있습니다
- 변화 강도를 아주 높게(0.9 이상) 두면 마스크 안쪽이 원본과 크게 달라집니다.
  0.7 근처가 무난합니다

---

## v0.3.3 (2026-08-24)

### 🐛 버그 수정

- **인페인팅 마스크 그리기에서 화면 전체가 빨갛게 덮여 브러시가 동작하지 않는 것처럼 보이던
  문제 수정.** 칠한 곳과 안 칠한 곳이 똑같이 보여 어디를 칠했는지 알 수 없었습니다. 마스크
  자체는 정상적으로 그려지고 있었고 화면에 겹쳐 보여 주는 방식만 잘못돼 있었습니다.
  이제 **칠한 영역만 반투명 빨강으로 표시**되고 나머지는 원본 그대로 보입니다

---

## v0.3.2 (2026-08-24)

### 🐛 버그 수정

- **저장 형식(WebP)과 갤러리 폴더를 바꿔도 적용되지 않던 문제 수정.** 옵션 창에서 고르고
  저장해도 값이 반영되지 않아, 계속 PNG로 저장되고 다시 열면 PNG로 돌아와 있었습니다.
  갤러리 폴더도 같은 이유로 무시되고 있었습니다
- **파일명의 `{negative_prompt}` 자리가 비어서 나오던 문제 수정.** 파일명 길이 상한(120자)에
  걸리면 뒤쪽이 통째로 잘려 나갔습니다. 프롬프트가 조금만 길어도 상한을 다 써버려
  네거티브 프롬프트가 사라졌습니다. 이제 프롬프트·네거티브 프롬프트·캐릭터가 상한을
  **나눠 쓰므로 넣은 토큰은 모두 파일명에 남습니다**
  (상한 안에 들어오는 짧은 파일명은 지금까지와 똑같이 나옵니다)

### 🧪 재발 방지

- 설정 항목을 추가하면서 옵션 창 반영 목록(`OWNED_FIELDS`) 등록을 빠뜨리면 실패하는
  테스트를 넣었습니다 — 위 두 항목이 실제로 이렇게 새어 나갔습니다

---

## v0.3.1 (2026-08-24)

### ✨ 신규 기능

**파일명 템플릿에 `{negative_prompt}` 토큰 추가** — 네거티브 프롬프트 앞부분을
파일명에 넣을 수 있습니다. 옵션 → 파일명의 단어 수 제한은 `{prompt}`와 함께 씁니다.

---

## v0.3.0 (2026-08-24)

### ✨ 신규 기능

**결과 미리보기 확대/축소** — 오른쪽 미리보기 화면에서 마우스 스크롤로 이미지를
확대·축소해 볼 수 있습니다.

- 위로 스크롤하면 커서 아래 지점을 중심으로 확대되고, 아래로 스크롤하면 다시 축소됩니다
- 뷰포트에 맞춘 크기 아래로는 축소되지 않습니다 — 이미 다 보이는 이미지라 의미가 없습니다
- 새 이미지가 생성되면 확대율은 다시 맞춘 크기로 돌아갑니다

**WebP 저장 옵션** — 옵션 → 파일명에서 저장 형식을 PNG/WebP 중 고를 수 있습니다.

- WebP는 무손실로 저장되어 화질 손실 없이 용량만 줄어듭니다
- 프롬프트·시드 등 생성 정보는 EXIF에 옮겨 담아 WebP로 저장해도 그대로 유지됩니다 —
  갤러리에서 "설정 재사용"도 PNG와 똑같이 동작합니다
- 기본값은 PNG(지금까지와 동일)이며, WebP로 저장한 이미지도 갤러리에 함께 보입니다

---

## v0.2.5 (2026-08-24)

### 🐛 버그 수정

- **갤러리(F3)가 흰 격자로만 보이던 문제 수정.** 썸네일을 그리는 코드가 항목마다 예외를
  내고 있었습니다. 창은 뜨고 개수도 맞는데 화면만 비어 보였고, 예외가 화면에 드러나지 않아
  v0.2.4의 창 수정으로도 증상이 그대로였습니다. 이제 썸네일이 실제로 나옵니다
- **오래된 V4 설정 파일(.txt)을 못 읽던 문제 수정.** 모델 항목이 없던 시절(V4 1.5.x)의
  파일을 열면 `Field required` 오류만 뜨고 아무것도 적용되지 않았습니다. 파일에 없는
  항목은 지금 화면 값을 그대로 두고 나머지만 적용합니다

### ✨ 신규 기능

**갤러리 폴더** — 갤러리가 볼 폴더를 따로 지정할 수 있습니다.

- 옵션 → 폴더에 `갤러리 폴더`가 생겼습니다. **비워 두면 지금처럼 결과 폴더를 봅니다**
- 갤러리 창 위쪽의 `📁 폴더 선택`으로 그때그때 다른 폴더를 열어 볼 수도 있습니다
  (이렇게 고른 폴더는 설정을 바꾸지 않아, 창을 다시 열면 설정한 폴더로 돌아옵니다)
- 하위 폴더까지 훑습니다 — V4 시절 날짜별로 나눠 둔 이미지도 한 번에 보입니다
- 지금 보고 있는 폴더가 창에 표시됩니다. 비어 있을 때 어디를 보고 있었는지 알 수 있습니다

---

## v0.2.4 (2026-08-24)

### 🐛 버그 수정

- **갤러리(F3)가 흰 사각형으로 뜨고 닫히지 않던 문제 수정.** 창이 아니라 메인 화면 안의
  위젯으로 그려져 제목표시줄도 닫기 버튼도 없었습니다. 이제 독립 창으로 뜨고 Esc로도 닫힙니다

### ✨ 신규 기능

**폴더 열기** — V4와 같은 단축키로 자주 쓰는 폴더를 바로 엽니다.

- `F5` 생성 결과 · `F6` 와일드카드 · `F7` 프리셋 (메뉴의 `폴더`에도 있습니다)
- 폴더가 없으면 만들어서 엽니다. 위치는 옵션 → 폴더에서 바꾼 값을 따릅니다

**설정 저장 / 불러오기** — 현재 생성 설정을 파일 하나로 남기고 되불러옵니다.

- `Ctrl+S` 저장 · `Ctrl+O` 불러오기 (V4는 불러오기가 `Ctrl+L`이었지만 V5에선 로그 보기입니다)
- 프롬프트·해상도·스텝·가이던스·시드까지 함께 저장됩니다. 시드가 랜덤이면 랜덤인 채로 둡니다
- **V4에서 쓰던 설정 파일(.txt)도 그대로 읽습니다.** V5에 없는 V4 모델은 건너뛰고 나머지만
  적용되므로, 현재 쓰던 모델은 바뀌지 않습니다

**리스케일 입력란** — 이미지 옵션에 `Prompt Guidance Rescale`을 추가했습니다.

- 지금까지는 모델 기본값으로 고정되어 나갔습니다. 이제 직접 조절하고 저장·복원됩니다
- PNG를 창에 끌어다 놓아 설정을 복원할 때도 이 값이 함께 들어옵니다

---

## v0.2.3 (2026-08-23)

### 🐛 버그 수정

- **잔액이나 새 버전을 확인하는 도중 창을 닫으면 앱이 비정상 종료되던 문제 수정.**
  조회는 백그라운드에서 도는데, 그 사이 창이 닫히면 결과를 전달할 곳이 사라져 프로세스가
  그대로 죽었습니다. 오류 창도 뜨지 않아 그냥 종료된 것처럼 보였습니다

### 🔧 릴리스

- 패치 노트를 릴리스 본문에 자동 반영 — 이 릴리스부터 적용됩니다.
  앞으로 이 자리에 그 버전의 변경 사항이 그대로 실립니다

---

## v0.2.2 (2026-08-23)

### 🐛 버그 수정

**시작하자마자 죽던 문제** (`ui/app.py`, `ui/main_window.py`)
- v0.2.1이 실행 즉시 `TypeError: 'bool' object is not callable`로 종료되던 문제 수정
- 원인: `NAISession.is_logged_in`은 프로퍼티인데 `is_logged_in()`으로 호출
- 테스트가 이를 놓친 이유: 테스트 더블이 같은 이름을 메서드로 흉내내고 있었다
- 메인 윈도우가 로그인 상태를 초기 버튼 상태에 반영하지 않던 문제도 함께 수정

### 🧪 재발 방지

- 테스트 더블의 공개 멤버가 실제 클래스와 같은 종류(프로퍼티/메서드)인지 확인하는 테스트 추가
- 시작 시 로그인 절차를 `ensure_login()`으로 분리하고 **실제 `NAISession`으로** 검증
  (저장된 토큰 / 거부된 토큰 / 취소 / 기억하지 않기)
- `--smoketest` 추가 — 프로즌 빌드에서 메인 윈도우와 옵션 창을 실제로 세워 본다.
  릴리스 워크플로가 `--selftest` 다음에 돌리므로, 뜨지 않는 빌드는 릴리스로 나갈 수 없다

---

## v0.2.1 (2026-08-23)

> ⚠️ 이 버전은 실행 즉시 종료되는 문제가 있습니다. v0.2.2를 사용해 주세요.

### 🐛 버그 수정

**옵션 창이 비어 있던 문제** (`ui/options_pages/__init__.py`, `ui/app.py`)
- 배포본에서 옵션 창을 열면 좌측 목록과 내용이 모두 비어 있던 문제 수정
- 원인: 옵션 페이지를 동적으로 발견해 PyInstaller가 페이지 모듈을 번들에 넣지 못했다
- `--selftest`가 옵션 페이지 등록까지 확인하도록 추가

### ✨ 신규 기능

**로그인 / 로그아웃** (`ui/login_dialog.py`, `ui/main_window.py`, `ui/app.py`)
- `파일` → `로그인` (`Ctrl+I`) 메뉴 추가 — V4와 같은 자리, 같은 단축키
- 로그인 상태에서 열면 로그아웃 버튼이 나온다. 로그아웃하면 세션과 저장된 토큰을 함께 지운다
- 시작할 때 로그인을 취소해도 앱이 뜬다 (로그아웃 상태). 생성 버튼은 잠기고 상태바가 알린다
- 토큰 검증이 실패하면 세션을 되돌린다 — 못 쓰는 토큰이 남아 로그인된 것처럼 보이던 문제 수정

### 📦 배포

- **단일 실행 파일 추가** — 폴더형 zip과 함께 `...-onefile.exe`도 릴리스에 올라간다 (V4와 같다).
  단일 파일은 실행할 때마다 내용을 임시 폴더에 풀어 시작이 5~10초 걸린다

---

## v0.2.0 (2026-08-23)

첫 Windows 배포본입니다.

### ✨ 신규 기능

**자동 빌드와 릴리스** (`.github/workflows/release.yml`, `packaging/`)
- 태그를 밀거나 Actions에서 실행하면 Windows 실행 파일이 만들어져 릴리스에 올라간다
- 빌드된 실행 파일에 `--selftest`를 돌려 번들 리소스와 keyring 백엔드를 실제로 확인한다
- 태그와 코드의 버전이 다르면 빌드를 실패시켜 어긋난 릴리스를 막는다

**앱 내 새 버전 확인** (`core/updates.py`, `ui/main_window.py`)
- `기타` → `새 버전 확인`. 시작할 때 자동 확인은 새 버전이 있을 때만 상태바에 조용히 알린다
- 옵션 → 인터페이스에서 자동 확인을 끌 수 있다

**퀵 매수 버튼** (`ui/main_window.py`, `core/settings/schema.py`)
- 생성 바의 `5장 / 10장 / 30장 / 200장`을 누르면 그 매수로 바로 연속 생성을 시작한다
- 네 값은 옵션 → 생성에서 바꾼다

### 🐛 버그 수정

- 태그 자동완성이 동작하지 않던 문제 수정 — Danbooru 태그 DB를 앱에 내장하고,
  `tag[count]` 형식을 읽지 못하던 파서를 고쳤다
- 모델 목록에 V5 모델 2종만 표시 (V4 모델은 메타데이터 재사용에만 쓴다)
- 캐릭터 프롬프트 영역을 기본 프롬프트 바로 아래로 이동
- 메인 창의 폴더 설정 UI 제거 (옵션에 있다)

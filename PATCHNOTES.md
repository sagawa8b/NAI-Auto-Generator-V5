# NAI Auto Generator V5 패치 노트

새 버전을 낼 때 이 파일 맨 위에 `## v버전 (날짜)` 절을 추가합니다. 릴리스 워크플로가 태그와
같은 이름의 절을 뽑아 릴리스 본문에 넣으므로, **절이 없으면 릴리스가 실패합니다**
(`tests/core/test_patchnotes.py`가 CI에서 먼저 잡습니다).

---

## v0.2.5 (2026-08-24)

### 🐛 버그 수정

- **갤러리(F3)가 흰 격자로만 보이던 문제 수정.** 썸네일을 그리는 코드가 항목마다 예외를
  내고 있었습니다. 창은 뜨고 개수도 맞는데 화면만 비어 보였고, 예외가 화면에 드러나지 않아
  v0.2.4의 창 수정으로도 증상이 그대로였습니다. 이제 썸네일이 실제로 나옵니다
- **오래된 V4 설정 파일(.txt)을 못 읽던 문제 수정.** 모델 항목이 없던 시절(V4 1.5.x)의
  파일을 열면 `Field required` 오류만 뜨고 아무것도 적용되지 않았습니다. 파일에 없는
  항목은 지금 화면 값을 그대로 두고 나머지만 적용합니다

### ✨ 신규 기능

**갤러리 폴더** — 갤러리가 볼 폴더를 따로 지정할 수 있습니다.

- 옵션 → 폴더에 `갤러리 폴더`가 생겼습니다. **비워 두면 지금처럼 결과 폴더를 봅니다**
- 갤러리 창 위쪽의 `📁 폴더 선택`으로 그때그때 다른 폴더를 열어 볼 수도 있습니다
  (이렇게 고른 폴더는 설정을 바꾸지 않아, 창을 다시 열면 설정한 폴더로 돌아옵니다)
- 하위 폴더까지 훑습니다 — V4 시절 날짜별로 나눠 둔 이미지도 한 번에 보입니다
- 지금 보고 있는 폴더가 창에 표시됩니다. 비어 있을 때 어디를 보고 있었는지 알 수 있습니다

---

## v0.2.4 (2026-08-24)

### 🐛 버그 수정

- **갤러리(F3)가 흰 사각형으로 뜨고 닫히지 않던 문제 수정.** 창이 아니라 메인 화면 안의
  위젯으로 그려져 제목표시줄도 닫기 버튼도 없었습니다. 이제 독립 창으로 뜨고 Esc로도 닫힙니다

### ✨ 신규 기능

**폴더 열기** — V4와 같은 단축키로 자주 쓰는 폴더를 바로 엽니다.

- `F5` 생성 결과 · `F6` 와일드카드 · `F7` 프리셋 (메뉴의 `폴더`에도 있습니다)
- 폴더가 없으면 만들어서 엽니다. 위치는 옵션 → 폴더에서 바꾼 값을 따릅니다

**설정 저장 / 불러오기** — 현재 생성 설정을 파일 하나로 남기고 되불러옵니다.

- `Ctrl+S` 저장 · `Ctrl+O` 불러오기 (V4는 불러오기가 `Ctrl+L`이었지만 V5에선 로그 보기입니다)
- 프롬프트·해상도·스텝·가이던스·시드까지 함께 저장됩니다. 시드가 랜덤이면 랜덤인 채로 둡니다
- **V4에서 쓰던 설정 파일(.txt)도 그대로 읽습니다.** V5에 없는 V4 모델은 건너뛰고 나머지만
  적용되므로, 현재 쓰던 모델은 바뀌지 않습니다

**리스케일 입력란** — 이미지 옵션에 `Prompt Guidance Rescale`을 추가했습니다.

- 지금까지는 모델 기본값으로 고정되어 나갔습니다. 이제 직접 조절하고 저장·복원됩니다
- PNG를 창에 끌어다 놓아 설정을 복원할 때도 이 값이 함께 들어옵니다

---

## v0.2.3 (2026-08-23)

### 🐛 버그 수정

- **잔액이나 새 버전을 확인하는 도중 창을 닫으면 앱이 비정상 종료되던 문제 수정.**
  조회는 백그라운드에서 도는데, 그 사이 창이 닫히면 결과를 전달할 곳이 사라져 프로세스가
  그대로 죽었습니다. 오류 창도 뜨지 않아 그냥 종료된 것처럼 보였습니다

### 🔧 릴리스

- 패치 노트를 릴리스 본문에 자동 반영 — 이 릴리스부터 적용됩니다.
  앞으로 이 자리에 그 버전의 변경 사항이 그대로 실립니다

---

## v0.2.2 (2026-08-23)

### 🐛 버그 수정

**시작하자마자 죽던 문제** (`ui/app.py`, `ui/main_window.py`)
- v0.2.1이 실행 즉시 `TypeError: 'bool' object is not callable`로 종료되던 문제 수정
- 원인: `NAISession.is_logged_in`은 프로퍼티인데 `is_logged_in()`으로 호출
- 테스트가 이를 놓친 이유: 테스트 더블이 같은 이름을 메서드로 흉내내고 있었다
- 메인 윈도우가 로그인 상태를 초기 버튼 상태에 반영하지 않던 문제도 함께 수정

### 🧪 재발 방지

- 테스트 더블의 공개 멤버가 실제 클래스와 같은 종류(프로퍼티/메서드)인지 확인하는 테스트 추가
- 시작 시 로그인 절차를 `ensure_login()`으로 분리하고 **실제 `NAISession`으로** 검증
  (저장된 토큰 / 거부된 토큰 / 취소 / 기억하지 않기)
- `--smoketest` 추가 — 프로즌 빌드에서 메인 윈도우와 옵션 창을 실제로 세워 본다.
  릴리스 워크플로가 `--selftest` 다음에 돌리므로, 뜨지 않는 빌드는 릴리스로 나갈 수 없다

---

## v0.2.1 (2026-08-23)

> ⚠️ 이 버전은 실행 즉시 종료되는 문제가 있습니다. v0.2.2를 사용해 주세요.

### 🐛 버그 수정

**옵션 창이 비어 있던 문제** (`ui/options_pages/__init__.py`, `ui/app.py`)
- 배포본에서 옵션 창을 열면 좌측 목록과 내용이 모두 비어 있던 문제 수정
- 원인: 옵션 페이지를 동적으로 발견해 PyInstaller가 페이지 모듈을 번들에 넣지 못했다
- `--selftest`가 옵션 페이지 등록까지 확인하도록 추가

### ✨ 신규 기능

**로그인 / 로그아웃** (`ui/login_dialog.py`, `ui/main_window.py`, `ui/app.py`)
- `파일` → `로그인` (`Ctrl+I`) 메뉴 추가 — V4와 같은 자리, 같은 단축키
- 로그인 상태에서 열면 로그아웃 버튼이 나온다. 로그아웃하면 세션과 저장된 토큰을 함께 지운다
- 시작할 때 로그인을 취소해도 앱이 뜬다 (로그아웃 상태). 생성 버튼은 잠기고 상태바가 알린다
- 토큰 검증이 실패하면 세션을 되돌린다 — 못 쓰는 토큰이 남아 로그인된 것처럼 보이던 문제 수정

### 📦 배포

- **단일 실행 파일 추가** — 폴더형 zip과 함께 `...-onefile.exe`도 릴리스에 올라간다 (V4와 같다).
  단일 파일은 실행할 때마다 내용을 임시 폴더에 풀어 시작이 5~10초 걸린다

---

## v0.2.0 (2026-08-23)

첫 Windows 배포본입니다.

### ✨ 신규 기능

**자동 빌드와 릴리스** (`.github/workflows/release.yml`, `packaging/`)
- 태그를 밀거나 Actions에서 실행하면 Windows 실행 파일이 만들어져 릴리스에 올라간다
- 빌드된 실행 파일에 `--selftest`를 돌려 번들 리소스와 keyring 백엔드를 실제로 확인한다
- 태그와 코드의 버전이 다르면 빌드를 실패시켜 어긋난 릴리스를 막는다

**앱 내 새 버전 확인** (`core/updates.py`, `ui/main_window.py`)
- `기타` → `새 버전 확인`. 시작할 때 자동 확인은 새 버전이 있을 때만 상태바에 조용히 알린다
- 옵션 → 인터페이스에서 자동 확인을 끌 수 있다

**퀵 매수 버튼** (`ui/main_window.py`, `core/settings/schema.py`)
- 생성 바의 `5장 / 10장 / 30장 / 200장`을 누르면 그 매수로 바로 연속 생성을 시작한다
- 네 값은 옵션 → 생성에서 바꾼다

### 🐛 버그 수정

- 태그 자동완성이 동작하지 않던 문제 수정 — Danbooru 태그 DB를 앱에 내장하고,
  `tag[count]` 형식을 읽지 못하던 파서를 고쳤다
- 모델 목록에 V5 모델 2종만 표시 (V4 모델은 메타데이터 재사용에만 쓴다)
- 캐릭터 프롬프트 영역을 기본 프롬프트 바로 아래로 이동
- 메인 창의 폴더 설정 UI 제거 (옵션에 있다)

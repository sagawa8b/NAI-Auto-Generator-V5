# NAI Auto Generator V5 사용자 매뉴얼

**버전:** 0.8.0
**최종 업데이트:** 2026년 8월

> 이 매뉴얼은 [V4의 `MANUAL_KR.md`](https://github.com/sagawa8b/NAI-Auto-V4/blob/main/MANUAL_KR.md)를
> 바탕으로, V5에 실제로 있는 기능만 골라 새로 썼습니다. V4에는 있지만 V5에는 아직 없는 기능
> (Character Reference, Vibe Transfer 등)은 마지막 [아직 안 되는 기능](#아직-안-되는-기능)
> 절에 따로 정리했습니다.

---

## 📋 목차

1. [개요](#개요)
2. [설치와 실행](#설치와-실행)
3. [기본 기능](#기본-기능)
   - [로그인](#로그인)
   - [프롬프트 입력](#프롬프트-입력)
   - [이미지 생성](#이미지-생성)
   - [설정 저장 및 불러오기](#설정-저장-및-불러오기)
   - [Options(옵션) 설정](#options옵션-설정)
4. [주요 기능](#주요-기능)
   - [캐릭터 프롬프트](#캐릭터-프롬프트)
   - [캐릭터 프롬프트](#캐릭터-프롬프트)
   - [강화 업스케일 (Enhance)](#강화-업스케일-enhance)
   - [아티스트 조합](#아티스트-조합)
   - [그림체 아레나](#그림체-아레나)
   - [세팅별 연속 생성](#세팅별-연속-생성)
   - [프리셋](#프리셋)
   - [갤러리 뷰](#갤러리-뷰)
   - [WD14 자동 태깅](#wd14-자동-태깅)
   - [프롬프트 어시스턴트 (`Ctrl+G`)](#프롬프트-어시스턴트-ctrlg)
   - [LLM 프롬프트 세트 (지시문 직접 고치기)](#llm-프롬프트-세트-지시문-직접-고치기)
   - [이미지에서 설정 불러오기](#이미지에서-설정-불러오기)
   - [로그 뷰어 / 새 버전 확인](#로그-뷰어--새-버전-확인)
5. [와일드카드 시스템](#와일드카드-시스템)
6. [화면 레이아웃 조절](#화면-레이아웃-조절)
   - [곁창 다루기 (최소화 · 최대화)](#곁창-다루기-최소화--최대화)
7. [파일이 저장되는 곳](#파일이-저장되는-곳)
8. [단축키](#단축키)
9. [아직 안 되는 기능](#아직-안-되는-기능)
10. [문제 해결](#문제-해결)

---

## 개요

NAI Auto Generator V5는 NovelAI Diffusion **V5** 모델 전용 이미지 생성 자동화 데스크톱 앱입니다.
[V4](https://github.com/sagawa8b/NAI-Auto-V4)의 후속작으로 기초부터 새로 작성되었으며, V4의 프롬프트
작성 도구(와일드카드·아티스트 조합·태그 자동완성)와 도구 모음(갤러리·프리셋·WD14)을 이어받으면서
V5 모델에 맞춰 다시 다듬었습니다.

### 주요 특징

- **V5 모델 전용**: `nai-diffusion-5-full` / `nai-diffusion-5-curated` 두 모델만 다룹니다
- **프롬프트 작성 도구**: 태그·와일드카드 자동완성, 구문 강조, 와일드카드, 아티스트 조합, 프롬프트 랜덤 선택 문법
- **캐릭터 프롬프트**: 캐릭터마다 프롬프트를 따로 두고 캔버스에서 위치를 지정
- **i2i / 인페인팅**: 원본 이미지 기반 생성과 마스크 영역 재생성
- **연속 생성 도구**: 매수·간격 지정 연속 생성, 세팅별 연속 생성, 퀵 매수 버튼
- **결과 관리**: 갤러리 뷰, 프리셋, 이미지 정보 재사용
- **다국어**: 한국어 / English / 日本語 / 中文 4종, 재시작 없이 전환

---

## 설치와 실행

배포되는 실행 파일(예: `NAI-Auto-V5.exe` 또는 압축을 푼 `NAI-Auto-V5` 폴더의 실행 파일)을 그대로
실행하면 됩니다. 설치 과정이나 별도 설정 없이 바로 로그인 창이 뜹니다.

개발 환경에서 소스로 직접 실행하려면:

```bash
pip install -e .
nai-auto-v5          # 또는: python -m naiauto.ui.app
```

---

## 기본 기능

### 로그인

NovelAI 계정의 **영구 API 토큰(Persistent API Token)** 으로 로그인합니다. V4와 달리 아이디/비밀번호
로그인은 지원하지 않습니다.

#### 로그인 방법

1. **토큰 발급**
   - NovelAI 웹사이트 → `Settings` → `Account` → `Get Persistent API Token` 클릭
   - `pst-`로 시작하는 토큰을 복사

2. **로그인 창에 입력**
   - 앱을 처음 실행하면 로그인 창이 자동으로 뜹니다 (이후에는 `Files` → `Log in`, `Ctrl+I`)
   - 발급받은 토큰을 붙여넣기

3. **기억하기** (선택사항)
   - `기억하기` 체크박스를 켜면 OS 키링(Windows 자격 증명 관리자 / macOS 키체인 /
     Linux Secret Service)에 토큰이 저장되어 다음 실행부터 자동으로 로그인됩니다
   - 껐을 때는 저장된 토큰을 지웁니다

4. **로그인 확인**
   - 상태바에 로그인 완료 메시지가 뜨고 Anlas 잔액이 표시됩니다

#### 여러 계정 쓰기 (API 계정 관리)

계정을 여러 개 쓴다면 `Files` → `API 계정 관리`(`Ctrl+Shift+I`)에서 토큰을 **최대 4개**까지
등록해 두고 골라 쓸 수 있습니다.

- 각 칸에 `pst-` 토큰을 넣고 `저장 및 전환`을 누르면 그 토큰으로 실제 로그인해 본 뒤
  성공했을 때만 저장하고 전환합니다 — 이후 생성 요청은 즉시 그 계정으로 나갑니다
  (연속 생성 중에 눌러도 진행 중인 이미지는 그대로, 다음 요청부터 바뀝니다)
- 전환에 실패하면 쓰던 계정을 그대로 유지합니다
- `삭제`는 그 칸의 토큰만 지웁니다. 지금 쓰는 계정을 지우면 다음 실행 때 자동 로그인도
  하지 않습니다 (지금 세션은 그대로 유지됩니다)
- 토큰은 로그인 창과 마찬가지로 설정 파일이 아니라 OS 키링에만 저장됩니다

#### 주의사항

- 토큰은 NovelAI 서버로만 전송되며, 본 앱의 서버는 존재하지 않습니다
- 토큰은 설정 파일에 평문으로 저장되지 않고 OS 키링에만 저장됩니다
- 로그인을 취소해도 앱은 로그아웃 상태로 뜨며, 언제든 `Ctrl+I`로 다시 로그인할 수 있습니다

---

### 프롬프트 입력

프롬프트 입력 영역은 **탭(Tab) UI**로, `Prompt` 탭과 `Negative Prompt` 탭을 오갑니다.

#### 기본 작성

```
1girl, blue eyes, long hair, standing, outdoor, smile, masterpiece, best quality
```

#### 자동완성 (태그 · 와일드카드)

프롬프트·네거티브 프롬프트와 캐릭터 슬롯의 입력란 모두에서 뜹니다. `↑` `↓`로 고르고
`Enter`(또는 `Tab`)로 넣습니다. `Esc`를 누르면 목록이 닫힙니다.

| 입력한 것 | 뜨는 목록 |
|---|---|
| 두 글자 이상의 낱말 (`blue`) | **단부루 태그** — 게시물 수가 많은 순 |
| `__` | **와일드카드 이름** — 와일드카드 폴더에 있는 `.txt` 파일 전부 |
| `__ha` | 이름에 `ha`가 든 와일드카드만 (앞에서 걸리는 것이 먼저) |
| `__=` / `##` | 같은 목록을 각각 공유 랜덤 · 루프카드 문법으로 |

와일드카드는 고르면 **닫는 기호까지 붙습니다** (`__ha` → `__hair__, `). 폴더를 열어 파일
이름을 확인하지 않아도 되고, 하위 폴더에 넣어 둔 것도 `__style/artist__`처럼 경로째 나옵니다.
앱을 켜 둔 채 파일을 새로 넣어도 잠시 뒤부터 목록에 뜹니다 (재시작하지 않아도 됩니다).

쓰던 줄 **도중**에 쳐도 뜹니다 — `1girl, blue hair __ha`처럼 앞에 글자가 있어도 되고,
줄을 바꿔 이어 써도 됩니다. 앞 토큰과의 경계는 쉼표 · 줄바꿈 · `|`(캐릭터 구분자)입니다.

태그도 마찬가지로 **쉼표를 찍지 않고 이어 써도** 뜹니다. `blue ey` → `blue eyes`처럼
낱말이 여럿인 태그를 위해 구간 전체로 먼저 찾고, 거기서 아무것도 없으면 **마지막 낱말**로
한 번 더 찾습니다 (`1girl, solo masterpi` → `masterpiece`). 이때 갈아 끼우는 것도 그
낱말뿐이라 앞에 쓴 글자는 그대로 남습니다.

> **끄고 켜는 자리는 둘로 나뉘어 있습니다** — 읽어 오는 곳이 다르기 때문입니다.
> 둘 다 `옵션 → 자동완성` 한 화면에 있고, **스위치는 따로**입니다 — 읽어 오는 곳이
> 다르기 때문입니다. `태그 자동완성 사용`은 같은 화면에서 지정하는 **태그 DB 파일**을,
> `와일드카드 자동완성 사용`은 `옵션 → 폴더 경로`의 **와일드카드 폴더**를 읽습니다.
> 한쪽을 꺼도 다른 쪽은 그대로 뜹니다.

#### 프롬프트 고급 문법

##### 1. 가중치 강조

NovelAI 자체 문법인 중괄호/대괄호(`{tag}`/`[tag]`)는 그대로 서버로 전달되어 웹 UI와 동일하게
작동합니다. 이와 별개로 앱이 자체적으로 처리하는 **콜론 강조** 문법도 있습니다.

```
1.5::big eyes::     1.5배 강조
0.5::feet::         0.5배 약화
::artist:name::     숫자 없이 강조만
```

입력창에서 강조(>1.0)는 파란 굵은 글씨, 약화(<1.0)는 회색으로 자동 표시되고, 짝이 맞지 않는
괄호(`(){}[]<>`)는 빨간색으로 표시됩니다. 강조/약화 색상은 `Files` → `Option` → `인터페이스`에서
바꿀 수 있습니다.

##### 2. 랜덤 선택 문법 — `<a|b|c>`

매 생성마다 꺾쇠 안의 후보 중 하나를 앱이 미리 골라 서버로 보냅니다.

```
<red|blue|green> dress     → 꺾쇠 안에서 하나를 무작위 선택
1girl, <red|blue> hair     → 태그 일부만 묶어도 동작
<a|<b|c>>                  → 중첩 가능
```

> **맨 `|`는 앱이 건드리지 않습니다.** NovelAI V4/V5에서 `|`는 캐릭터 프롬프트 구분자이고
> (아래 3번), `||a|b|c||`는 NovelAI 공식 Prompt Randomizer입니다 — 둘 다 그대로 전달됩니다.
> v0.7.1까지 쓰던 `1girl, red|blue hair` 형태는 v0.7.2부터 랜덤이 아니므로
> `1girl, <red|blue> hair`로 바꿔 주세요.

##### 3. 프롬프트로 캐릭터 나누기 — `base | 캐릭터1 | 캐릭터2`

캐릭터 프롬프트 칸이 **모두 비어 있으면**, 메인 프롬프트를 `|`로 나눠 첫 조각을 베이스로,
나머지를 캐릭터 프롬프트로 보냅니다 (NovelAI 웹UI와 같은 규칙 — 칸을 하나라도 채우면 이
문법은 쓰지 않습니다).

```
2girls, outdoors |
girl, blonde hair, blue eyes |
girl, black hair, green eyes
```

- 이렇게 만든 캐릭터는 **좌표를 넘기지 않습니다** — 배치는 AI가 정합니다. 위치를 지정하려면
  캐릭터 프롬프트 칸을 쓰세요
- `||a|b|c||` 안쪽의 `|`는 구분자로 세지 않습니다
- 와일드카드가 만들어 낸 `|`도 잡힙니다 — 와일드카드 한 줄로 캐릭터 구성을 통째로 바꿀 수 있습니다

##### 4. 와일드카드 / 아티스트 조합

각각 [와일드카드 시스템](#와일드카드-시스템), [아티스트 조합](#아티스트-조합) 절 참고.

##### 5. 화면 비율 지시어 — `<res:...>`

메인 프롬프트나 와일드카드 항목에 `<res:portrait>` / `<res:square>` / `<res:wide>`를 쓰면
그 장은 **현재 해상도 등급의** 세로형 / 정방형 / 가로형 해상도로 뽑습니다. 프롬프트 내용과
비율을 묶어 둘 수 있어서, `해상도 랜덤`이 등급 안에서 아무거나 고르는 것과 다릅니다.

```
1girl, full body, standing, <res:portrait>
```

- 지시어는 NovelAI로 보내기 전에 프롬프트에서 **지웁니다** (빈 쉼표 구간도 정리합니다)
- 대소문자를 가리지 않고, 여러 개가 있으면 **마지막** 것을 씁니다
- `해상도 랜덤`보다 우선합니다. 지금 등급에 그 비율이 없으면(예: Wallpaper에는
  정방형이 없습니다) 랜덤으로 되돌리지 않고 현재 해상도를 그대로 씁니다
- i2i·인페인팅·[폴더 강화](#강화-업스케일-enhance)처럼 크기가 원본 이미지로 정해지는 경우에는
  해상도를 바꾸지 않습니다 (지시어를 지우기는 합니다)
- `<res:banana>`처럼 모르는 값은 지시어가 아니므로 프롬프트에 그대로 남습니다 — 오타를
  조용히 삼키는 것보다 눈에 띄는 편이 낫다고 봤습니다

---

### 이미지 생성

#### 좌측 패널 — 생성 파라미터

| 항목 | 설명 |
|---|---|
| **모델** | `NAI Diffusion 5 Full`(기본, 권장) / `NAI Diffusion 5 Curated` 중 선택 |
| **해상도** | 모델 등급별 프리셋(Normal / Large / Wallpaper) 드롭다운 + 직접 입력. Large·Wallpaper와 사용자 정의 해상도 6칸은 옵션에서 켤 수 있고, 무료 티어를 넘으면 경고가 붙습니다 |
| **해상도 랜덤** | 켜면 현재 등급의 세로/가로/정방형 중 하나를 매 이미지마다 무작위로 사용 (i2i·인페인팅으로 해상도가 고정된 동안은 무시) |
| **샘플러 / 노이즈 스케줄** | 생성 알고리즘과 노이즈 적용 방식 |
| **Steps(스텝)** | 기본 28, 높을수록 정밀하지만 시간 증가 |
| **Prompt Guidance(CFG)** | 기본 5.0 — 낮으면 창의적, 높으면 프롬프트에 충실 |
| **Prompt Guidance Rescale** | 0.0~1.0, 기본 0 |
| **Seed(시드)** | 랜덤(기본) 또는 고정. 고정 시드로 2장 이상 연속 생성은 막힙니다(같은 그림만 반복되므로). 고정 시드로 같은 값을 두 번 생성하면 [동일 조건 재생성](#동일-조건-재생성) 옵션이 개입합니다 |
| **UC 프리셋** | heavy / light / human focus / none 중 선택 |
| **품질 태그** | 켜면 모델이 권장하는 품질 태그 문구를 프롬프트 뒤에 자동으로 붙입니다 |

#### 생성 버튼 (생성 바)

생성 바는 좌측 패널 아래, 스크롤과 무관하게 항상 같은 자리에 고정됩니다.

| 버튼 | 기능 |
|---|---|
| **1회 생성** | 현재 설정으로 이미지 1장 생성 |
| **연속 생성** | 매수 입력란의 값만큼 연속 생성 (0 = 무제한, 중지 버튼으로 종료) |
| **세팅별 연속 생성** | 세팅 파일 2개 이상을 순환하며 연속 생성. [자세히](#세팅별-연속-생성) |
| **중지** | 진행 중인 연속 생성을 멈춥니다 |
| **퀵 매수 버튼** | 기본 5 / 10 / 30 / 200장 — 누르면 매수 입력란에 그 값을 넣고 바로 연속 생성을 시작합니다. 값은 옵션 → 생성에서 바꿀 수 있습니다 |

요청은 항상 하나씩 순서대로 나갑니다. 레이트리밋(429)에 걸리면 서버가 알려준 대기시간만큼(최대
5회) 기다렸다가 같은 이미지를 다시 시도하고, 일시적 네트워크/서버 오류는 5→10→20초 간격으로
최대 3회 재시도합니다. Anlas 부족이나 인증 실패 같은 오류는 바로 중단됩니다.

**연속 생성 중 프롬프트를 고치면?** 진행 중인 이미지에는 영향이 없고, **다음 이미지부터** 새
프롬프트·네거티브·캐릭터 프롬프트가 반영됩니다. 해상도도 마찬가지입니다 — 배치를 멈추지 않고도
중간에 값을 바꿔 가며 계속 생성할 수 있습니다.

#### 결과 패널

- 결과 이미지는 마우스 스크롤로 확대/축소됩니다
- 상태바에 진행 상황(`연속 생성 중 (2/5)`, `다음 생성 대기 중...`), **V5 생성 크레딧 게이지**
  (잔량 %와 충전률 툴팁), **Anlas 잔액**이 함께 표시됩니다
- 결과 이미지 위에 마우스를 올리면 저장 경로가 툴팁으로 뜹니다

---

### 설정 저장 및 불러오기

현재 화면의 모든 생성 파라미터(프롬프트, 캐릭터 프롬프트, 모델, 해상도, 샘플러, 시드 등)를 파일
하나로 저장하고 불러올 수 있습니다.

- **저장**: `Files` → `Save Settings` (`Ctrl+S`) — `.json` 또는 `.txt`로 저장
- **불러오기**: `Files` → `Load Settings` (`Ctrl+O`)

V4가 저장한 `.txt` 설정 파일도 그대로 읽을 수 있습니다 (V4에 없던 항목은 지금 화면 값을 유지).
불러온 파일에 V5에 없는 모델·샘플러가 적혀 있으면 해당 항목만 건너뜁니다.

---

### Options(옵션) 설정

`Files` → `Option` (`Ctrl+,`)에서 엽니다. **저장을 눌러야 반영**되고 취소는 아무것도 바꾸지
않습니다.

| 페이지 | 내용 |
|---|---|
| **폴더 경로** | 결과 · 와일드카드 · 프리셋 · 아티스트 조합 · 갤러리 폴더 경로, WD14 태거 모델(폴더·모델·다운로드) |
| **파일명** | 파일명 템플릿, 프롬프트/캐릭터 프롬프트 단어 수 제한 |
| **생성** | 기본 생성 매수·간격, 퀵 매수 버튼 4개의 값, 동일 조건 재생성 동작 |
| **해상도** | Large / Wallpaper 등급 사용 여부, 사용자 정의 해상도 6칸 |
| **인터페이스** | 언어, i2i 패널 기본 표시 여부, 프롬프트 입력란 글꼴 크기·색상·강조색, 접이식 섹션 초기화 |
| **자동완성** | 태그 자동완성 사용 여부와 태그 DB 경로(비우면 동봉 DB 사용), 와일드카드 자동완성 사용 여부 |
| **로그** | 로그 폴더, 디버그 로그, API 응답 헤더 기록 여부 |

#### 동일 조건 재생성

이미지를 드래그&드롭하거나 이미지 정보 창에서 설정을 불러오면 **시드까지** 그대로 복원됩니다.
그 상태에서 생성을 두 번 누르면 완전히 같은 요청이 두 번 나가 **같은 이미지에 크레딧이 두 번**
나갑니다. 옵션 → 생성의 **`동일 조건으로 다시 생성할 때`** 가 이때의 동작을 정합니다.

| 선택 | 동작 |
|---|---|
| **시드를 자동으로 랜덤 변경** (기본) | 시드만 새로 뽑아 그대로 생성합니다. 바뀐 시드는 시드 입력란과 상태바에 표시됩니다 |
| **그대로 생성** | 아무것도 하지 않습니다 (같은 이미지가 한 장 더 나옵니다) |
| **경고하고 생성하지 않음** | 경고 창을 띄우고 멈춥니다. 시드를 바꾸거나 `랜덤`을 켜면 다시 생성됩니다 |

판정 기준은 **직전에 완성한 이미지**의 요청과 프롬프트·시드·해상도·스텝·CFG·샘플러·캐릭터
프롬프트·i2i 원본까지 전부 같은 경우입니다. 다음은 같은 요청이어도 결과가 달라지므로 개입하지
않습니다.

- 시드가 **`랜덤`** 인 경우 (애초에 매 장 다른 시드입니다)
- 프롬프트에 **와일드카드**(`__name__`), **꺾쇠 랜덤**(`<a|b>`), **아티스트 조합**(`{artist:그룹}`),
  **NovelAI Prompt Randomizer**(`||a|b||`)가 있는 경우
- 생성이 **실패**해서 이미지가 나오지 않은 경우 (같은 값으로 재시도할 수 있어야 합니다)

#### 파일명 템플릿

기본값 `{datetime}_{seed}`이며, 다음 토큰을 조합할 수 있습니다.

| 토큰 | 내용 |
|---|---|
| `{datetime}` / `{date}` / `{time}` | 날짜·시간 |
| `{prompt}` / `{negative_prompt}` | 프롬프트 / 네거티브 프롬프트 (남길 단어 수는 옵션에서 지정) |
| `{character}` | 첫 번째 캐릭터 프롬프트 |
| `{seed}` / `{model}` | 시드 값 / 모델 이름 |

---

## 주요 기능

### 캐릭터 프롬프트

캐릭터마다 프롬프트와 네거티브 프롬프트를 따로 두고, 화면에서 위치를 지정하는 기능입니다.

#### 사용법

1. **캐릭터 추가**: 캐릭터 프롬프트 영역의 `추가` 버튼 클릭 (슬롯마다 프롬프트/네거티브 탭이
   따로 있고, 태그·와일드카드 자동완성도 붙습니다)
2. **위치 지정**
   - 기본은 **수동 위치** — 생성 해상도 비율에 맞춘 캔버스에서 캐릭터 마커를 드래그해 위치를
     정합니다 (캐릭터가 2명 이상이거나, "수동 위치 지정"을 켜고 1명일 때)
   - `AI 위치 선택`을 켜면 좌표를 보내지 않고 AI가 배치를 정합니다
3. **위치 지정 패널 접기**: `위치 지정` 머리글을 누르면 캔버스를 접습니다. 접혀 있는 동안에는
   지금 좌표가 `1 C3 · 2 D3`처럼 한 줄 요약으로 남고, 좌표 자체는 그대로 유지됩니다. 자리를
   한 번 잡고 나면 캔버스를 접어 캐릭터 슬롯을 더 넓게 쓸 수 있습니다
4. **캔버스 높이 조절**: 캔버스 아래 드래그 핸들로 높이를 키우거나 줄입니다 (세션 간 유지)
5. **슬롯 크기 조절**: 각 슬롯 프롬프트 영역 아래 드래그 핸들로 높이를 조절할 수 있습니다. 한
   슬롯을 조절하면 모든 슬롯이 같은 높이로 맞춰집니다
6. **슬롯 접기**: 슬롯 왼쪽 끝의 화살표를 누르면 그 슬롯이 헤더 한 줄로 접히고, 프롬프트 앞
   40자가 미리보기로 남습니다. 값은 그대로 유지되므로 생성 결과는 달라지지 않습니다. 캐릭터가
   서너 명만 돼도 세로가 금방 차는데, 지금 손대지 않는 슬롯만 접어 두면 됩니다

캐릭터가 1명뿐이고 수동 위치 지정도 꺼져 있으면 좌표는 의미가 없어 중앙(0.5, 0.5)으로 고정됩니다.

---

### 프롬프트 결과 표시

생성 바의 **`프롬프트 결과 표시` 체크박스**(또는 보기 메뉴 / `F8`)를 켜면, **방금 생성한
이미지의 프롬프트와 설정**이 결과 이미지 아래쪽 40%에 반투명 판으로 겹쳐 표시됩니다
(V4의 같은 기능). 체크박스와 메뉴 항목은 서로 같은 상태를 가리킵니다.

보이는 내용은 화면의 입력란이 아니라 **그 장에 실제로 보낸 값**입니다. 와일드카드(`__파일__`),
아티스트 조합, `<a|b|c>` 랜덤 선택이 모두 전개된 뒤의 프롬프트라, 연속 생성 중 마음에 드는 그림이
나왔을 때 그게 어떤 조합이었는지 바로 확인할 수 있습니다.

| 표시 항목 | 내용 |
|---|---|
| 프롬프트 / 네거티브 | 전개 후 전문 (네거티브는 UC 프리셋까지 합쳐진 실제 값) |
| 캐릭터 N | 캐릭터별 프롬프트 · 네거티브 · 좌표 (`AI 위치 선택`이면 `(자동)`) |
| 모델 · 크기 · 시드 | 그 장에 쓰인 값 |
| 스텝 · Prompt Guidance · Rescale | |
| 샘플러 · 노이즈 스케줄 | |

판이 이미지를 가리므로 필요할 때만 켜는 쪽이 편합니다. 켜고 끈다고 이미지 크기는 달라지지
않으며, 켬/끔 상태는 종료 시 저장됩니다. 판 안에서 마우스 휠로 스크롤하면 긴 프롬프트도 끝까지
읽을 수 있습니다.

---

### Image to Image / 인페인팅

보기 → `Image to Image` (`F2`)로 패널을 켭니다.

1. **원본 이미지 선택** — `Select Image` 버튼 또는 드래그 앤 드롭
2. **파라미터**
   - **Strength(강도)**: 0.01~1.0, 기본 0.7 — 높을수록 원본에서 많이 변형
   - **Noise(노이즈)**: 0.0~0.99, 기본 0.0
3. **인페인팅으로 전환**: 마스크를 그리면 자동으로 인페인팅 모드가 됩니다
   - 마스크 편집기에서 브러시로 재생성할 영역을 칠합니다 (브러시 크기 4~300px, 지우개, 초기화
     지원 — V4의 8×8 그리드 단위가 아니라 자유로운 픽셀 단위입니다)
   - `Overlay Original Image`를 켜면 마스크 밖 영역을 원본 그대로 유지합니다 (기본은 꺼짐 —
     NovelAI 웹 UI 기본값과 동일)
4. 생성 크기는 항상 **원본 이미지 크기**를 따릅니다 (연속 생성 중 해상도 변경, 랜덤 해상도 모두
   i2i/인페인팅 중에는 적용되지 않습니다)

---

### 강화 업스케일 (Enhance)

이미 만든 그림을 다시 그려 크게 만드는 기능입니다. 보기 → `강화 업스케일` (`F4`)로 패널을
켭니다 (i2i 패널과는 동시에 켤 수 없습니다 — 둘 다 같은 img2img 요청을 만들기 때문입니다).

1. **원본 고르기** — `이미지 선택...`으로 파일을 고르거나, 방금 만든 그림이면 `최근 결과`를
   누릅니다. NovelAI 메타데이터가 남아 있는 PNG면 그 그림의 프롬프트·샘플러·스텝이 화면에
   그대로 채워집니다 (`원본의 생성 설정 사용` 체크박스로 끌 수 있습니다). **시드는 새로
   뽑습니다** — NovelAI 웹 UI도 강화할 때마다 새 시드를 씁니다
2. **확대 배율 고르기** — 패널이 결과 크기를 바로 계산해 보여 줍니다 (`832 × 1216 →
   1467 × 2144 (×1.76)`)

| 배율 | 하는 일 |
|---|---|
| **1x** | 크기는 그대로, 디테일만 다시 그립니다 |
| **1.5x** | 강화 해상도로 **먼저 키운 뒤** 그 해상도에서 다시 그립니다 (832×1216 → 1280×1856). 확대 티를 지우려고 프롬프트 끝에 `-2::upscaled, blurry::,`를 자동으로 붙입니다 |
| **Max** | 원본 해상도에서 디테일만 다시 그리고, **NovelAI 서버가** 결과를 최대 크기까지 키웁니다 (832×1216 → 1467×2144, 1024×1024 → 1773×1773). 확대를 서버가 하므로 프롬프트는 건드리지 않습니다 |

> **고를 수 없는 배율은 잠기고, 왜인지 패널이 알려 줍니다.**
>
> - **상한 초과** — 강화 결과에는 약 3.1MP(1536×2048) 상한이 있어서, Large(1536×1024 등)와
>   Wallpaper 해상도는 1.5x가 이 상한을 넘습니다. NovelAI 웹 UI도 똑같이 1.5x를 감춥니다
> - **사용자 정의 해상도** — 1.5x는 NovelAI가 강화 해상도를 정해 둔 표준 크기
>   (1024×1024, 832×1216, 1216×832, 640×640, 512×768, 768×512)에서만 쓸 수 있습니다.
>   그 밖의 크기에서 NovelAI가 어떤 해상도를 쓰는지 알 수 없어, 짐작한 크기로 Anlas를
>   쓰는 대신 잠급니다
>
> **Max는 어떤 크기에서도 쓸 수 있습니다** — 가로세로비를 지켜 상한까지 키우는 계산이라
> 표에 의존하지 않습니다. 폴더 강화도 시작 전에 대기열을 확인해서, 고른 배율로 처리할 수
> 없는 파일이 섞여 있으면 한 장도 보내지 않고 알려 줍니다.

3. **변화 강도 / 노이즈** — 웹 UI의 `Magnitude`는 이 두 값의 프리셋일 뿐이고, 웹 UI에서도
   `Show Advanced`를 누르면 같은 두 값이 나옵니다 (V4와 같은 방식). 기본값 0.5 / 0은
   웹 UI의 Magnitude 2와 같은 값입니다. 높이면 원본에서 더 많이 벗어납니다
4. **생성** — 아래 생성 바의 `1회 생성`을 누르면 강화가 시작됩니다

#### 폴더 통째로 강화하기

`폴더 선택...`으로 폴더를 고르면 그 안의 이미지를 훑어 대기열을 만들고, `폴더 강화 시작`을
누르면 한 장씩 강화합니다. **이미지마다 자기 메타데이터의 프롬프트와 자기 크기**로 강화하며,
생성 간격·중지 버튼은 일반 연속 생성과 똑같이 동작합니다. 메타데이터가 없는 이미지는 화면에
있는 프롬프트로 강화합니다.

> **Anlas 주의** — 강화도 일반 생성과 같은 비용이 듭니다. 1.5x와 Max는 해상도가 커서
> 더 비쌉니다 (실측 1건: 1024×1024를 Max로 강화하니 **37 Anlas**가 나갔습니다).
> 폴더 강화는 장수만큼 그대로 나가니 대기열 수를 확인하고 시작하세요.

---

### 아티스트 조합

여러 아티스트 태그를 그룹으로 묶어 랜덤 또는 순차로 치환하는 기능입니다.

```
{artist:그룹이름}        그룹에서 무작위로 하나 선택
{artist_loop:그룹이름}   생성할 때마다 순서대로 하나씩 (끝까지 가면 처음으로)
```

그룹은 옵션 → 폴더에서 지정한 **아티스트 조합 폴더**의 JSON 파일로 정의합니다.

```json
{ "그룹이름": ["artist:aaa", "artist:bbb", "artist:ccc"] }
```

현재는 이 JSON 파일을 텍스트 편집기로 직접 만들어야 하며, 앱 안에 편집 대화상자는 아직 없습니다.

---

### 그림체 아레나

작가 태그를 무작위로 조합해 **그림을 실제로 뽑고**, 그 그림을 둘씩 놓고 골라(이상형 월드컵)
취향을 점수로 남기고, 이긴 조합끼리 **교배**해 더 마음에 드는 그림체를 찾아 가는 기능입니다.
감으로 작가를 섞어 보는 대신 **기록으로 남는 순위표**를 만듭니다.

도구 → `그림체 아레나...`로 엽니다. 창은 모드리스라 메인 창을 막지 않습니다.

> 이 기능의 아이디어는 숭이717 님의
> [NAI 그림체 이상형 월드컵](https://arca.live/b/aiart/181388564)에서 왔습니다
> (원저자 허락을 받아 이식). 점수 매기는 방식 등 몇 곳은 다르게 잡았습니다.

#### 흐름

```
작가 명단 → 무작위 조합 생성 → 그림 생성 → 이상형 월드컵 → 상위 조합 교배 → 반복
                                   ↑                                    │
                                   └────────────────────────────────────┘
                                     자식 조합도 그림을 뽑아 다시 겨룹니다
```

다섯 개 탭이 이 순서를 그대로 따릅니다. 앞 단계의 결과가 다음 단계의 재료가 되므로,
처음에는 왼쪽부터 차례로 가면 됩니다. 한 바퀴 돌 때마다 세대(Gen)가 하나 올라갑니다.

#### 시작하기 전에

1. **로그인** — 아레나는 실제로 NovelAI에 이미지를 요청합니다.
2. **기본 프롬프트** — 메인 창에 구도·인물·품질 태그를 채워 둡니다. 아레나는 *작가 태그만*
   바꿔 가며 이 프롬프트로 그림을 뽑습니다.
3. **작가 목록** — 후보가 많을수록 조합의 폭이 넓어집니다. 10명 이상을 권합니다.

> **크레딧을 쓰는 버튼은 하나뿐입니다.** 조합을 만드는 것과 겨루게 하는 것은 공짜입니다.
> 실제로 Anlas / V5 크레딧을 소모하는 것은 조합 생성 탭의 **`그림 생성`** 하나뿐이고,
> 누르기 전에 몇 장을 뽑을지 알려 줍니다. `그림 자동 보충`도 있지만 **기본은 꺼짐**이라,
> 켜지 않으면 앱이 마음대로 크레딧을 쓰지 않습니다.

---

#### 1. 작가 명단

조합에 뽑힐 후보이자, 그 자체로 순위표입니다. 조합이 겨룰 때마다 거기 낀 작가도 점수를
나눠 받으므로, 여러 조합에 걸쳐 꾸준히 이긴 작가가 위로 올라옵니다.

**작가를 넣는 세 가지 방법**

| 방법 | 설명 |
|---|---|
| 이름 붙여넣기 | 줄바꿈이나 쉼표로 구분해 넣습니다 |
| 프롬프트 통째로 | 가중치가 붙은 완성 프롬프트를 그대로 넣어도 **작가 태그만** 골라냅니다.<br>`1girl, 1.6::artist:kuto tubuyaki::, masterpiece` → `kuto tubuyaki` |
| 그림 끌어다 놓기 | 이미지 파일을 창에 떨어뜨리면 **그 그림에 쓰인 작가**를 뽑아 넣습니다.<br>PNG tEXt · WebP EXIF · 스텔스 메타데이터를 모두 읽습니다 |

마음에 든 그림 100장을 한꺼번에 떨어뜨려도 됩니다. 프롬프트에 `artist:`가 하나도 없는
그림은 건너뜁니다 — 그러지 않으면 `1girl`이나 `solo` 같은 평범한 태그까지 작가로
등록됩니다.

**표에서 할 수 있는 일**

- **여러 줄 골라 한 번에 삭제** — `Ctrl` · `Shift`로 고르고 `Delete` 또는 `선택 삭제`.
- **이름 직접 수정** — 칸을 눌러 바로 고칩니다. 이미 있는 이름으로는 바뀌지 않습니다.
- **`_ → 공백`** — `kantoku_(artist)`를 `kantoku (artist)`로 일괄 정리합니다. 밑줄이 들어간
  이름이 진짜인 경우도 있어 자동으로 바꾸지는 않습니다.
- **가중치 고정** — `고정 가중치` 칸에 숫자를 넣으면 그 작가는 *항상* 그 세기로 들어갑니다.
  비워 두면 매번 새로 뽑습니다.
- **정렬** — 점수순 · 이름순 · 검출순. 점수순에서는 아직 안 겨뤄 본 작가가 아래로 갑니다
  (1000점은 실력이 아니라 "모름"입니다).

작가를 명단에서 지워도 **이미 만들어진 조합과 그림, 점수는 그대로 남습니다.** 그 조합의
평가는 여전히 유효하기 때문입니다.

---

#### 2. 조합 생성

작가를 섞어 조합을 만들고, 그 조합의 그림을 뽑습니다.

| 값 | 기본값 | 뜻 |
|---|---|---|
| 작가 수 | 3 ~ 6 | 한 조합에 넣을 작가 수의 최소~최대 (1~10) |
| 가중치 방식 | 균형 | `랜덤`(균등 난수) · `균형`(상위 약 25%가 중심을 잡고 나머지가 보조) · `커브`(자리 순서에 따른 곡선) |
| 커브 모양 | 하강 | 하강 · 상승 · 역V자 · V자 · 평탄 (방식이 `커브`일 때만) |
| 가중치 최소 ~ 최대 | 0.8 ~ 1.8 | 이 범위 안에서만 뽑습니다. 최소가 최대를 넘으면 서로 밀어 줍니다 |
| 증감 폭 | 0.05 | 가중치를 이 폭의 배수로 맞춥니다 (0.01 ~ 1). `0.01`이면 1.16·1.17처럼 잘게, `1`이면 정수로 나옵니다. 최소·최대 칸의 화살표도 이 폭으로 움직입니다 |
| 넣을 위치 | 프롬프트 앞 | 작가 블록을 프롬프트 **앞** / **뒤** / `<artist>` 자리 중 어디에 넣을지 |
| 한 번에 만들 조합 | 12 | `랜덤 조합 만들기` 한 번에 만들 개수 |
| 미리 뽑아 둘 여유분 | 4 | 겨룰 그림이 이 수 아래로 내려가면 자동 보충 대상이 됩니다 (자동 보충을 켠 경우) |
| 해상도 | 메인 창 설정 | 모델이 지원하는 크기만 고를 수 있습니다. 습작이니 작게 뽑아 아껴도 됩니다 |

같은 작가 구성의 조합은 두 번 만들어지지 않습니다. 명단이 작아 더 만들 수 없으면 요청한
개수보다 적게 나옵니다.

**만들어지는 프롬프트**

| 출력 | 규칙 |
|---|---|
| `1.2::artist:a, artist:b::` | 가중치가 같은 자리는 한 블록으로 묶습니다 |
| `artist:c` | 가중치 1.0은 래퍼 없이 태그만 — 의도치 않은 강조가 섞이지 않게 |
| `1.5::artist:matrix16 ::` | 숫자로 끝나는 태그는 닫는 `::` 앞에 공백을 넣습니다. 없으면 NovelAI가 `16::`을 가중치로 잘못 읽습니다 |

`랜덤 조합 만들기`는 조합만 만듭니다. 실제로 크레딧을 쓰는 것은 **`그림 생성`** 하나뿐입니다.

**미리보기**는 실제로 보낼 조건을 그대로 보여 줍니다 — 작가 조합 · 프롬프트 · 네거티브 ·
캐릭터 프롬프트. 이 넷은 모두 **메인 창의 것을 그대로** 씁니다. 매 장 달라지는 문법이
섞여 있으면 **어느 자리에 있는지까지** 알려 줍니다 (프롬프트는 멀쩡한데 캐릭터 프롬프트에
와일드카드가 있는 경우가 많습니다).

미리보기의 작가 조합은 **가장 최근에 만든 것**입니다. 규칙을 바꾼 뒤 다시 만들면 바뀐 값이
바로 보입니다. 이미 만들어 둔 조합은 그대로 남습니다 — 규칙은 새로 만드는 조합에만
적용됩니다.

**조합 큐**

만든 조합이 아래 표에 최신순으로 쌓입니다. 각 줄은 상태(`대기` / `그림 있음`) · 세대 ·
작가 구성을 보여 줍니다.

- 상태 칸은 `대기` · `그림 있음` · `그림 있음 · N전`으로 나뉩니다. **전적이 붙은 줄은 평가를
  담고 있다는 뜻**이니 지우기 전에 확인하세요.
- 여러 줄을 골라 `선택 삭제` 또는 `Delete`로 지웁니다 (표에 포커스가 있을 때만 듣습니다).
  그림이나 전적이 있는 조합이 섞여 있으면 먼저 묻습니다.
- `대기 중인 조합 모두 삭제`는 그림이 없는 것만 한 번에 치웁니다. 조건을 잘못 잡고 잔뜩
  만들었을 때 쓰며, **이미 그림을 뽑은 조합은 건드리지 않습니다** — 크레딧을 쓴 것들입니다.
- 생성 중에는 큐를 건드릴 수 없습니다. 뽑고 있는 조합을 지우면 방금 쓴 크레딧이 갈 곳을
  잃기 때문입니다.

**지운 것을 되돌리기**

`되돌리기`는 **마지막 삭제 한 번**을 되살립니다. 큐뿐 아니라 월드컵 카드의 `삭제`, 진화 탭의
`정리`로 지운 것도 같은 버튼으로 돌아옵니다. 점수·전적·세대가 모두 그대로 복구됩니다.

되돌릴 수 있는 이유는 **기본값이 그림 파일을 남기기** 때문입니다.

| `그림 파일도 휴지통으로` | 지울 때 | 되돌리면 |
|---|---|---|
| **꺼짐 (기본)** | 조합 기록만 지웁니다. 그림 파일은 폴더에 남습니다 | 그림까지 **온전히** 되살아납니다 |
| 켜짐 | 그림 파일을 **휴지통으로** 보냅니다 | 점수·전적은 살아나지만 그림은 없어 `대기`가 됩니다 |

남은 그림 파일은 어떤 조합도 가리키지 않으므로, 통계 탭의 `안 쓰는 그림 정리`로 언제든
치울 수 있습니다. 디스크가 아깝지 않다면 기본값(꺼짐) 그대로 두는 편이 안전합니다.

**그림 파일은 바로 지우지 않고 휴지통으로 보냅니다.** 크레딧을 써서 뽑은 것이라,
`되돌리기`를 놓쳤어도 Windows 휴지통에서 되찾을 수 있게 한 것입니다 (macOS는 `~/.Trash`,
Linux는 freedesktop 휴지통). 통계 탭의 `안 쓰는 그림 정리`도 같습니다. 네트워크 드라이브처럼
휴지통을 쓸 수 없는 자리에서만 바로 지우고, 그 사실을 로그에 남깁니다.

> 작가 명단에서 **가중치를 고정**해 둔 작가는 예외입니다. 고정값은 사용자가 직접 적은
> 값이라 범위와 증감 폭을 모두 무시하고 그대로 들어갑니다.

> **한 세션의 모든 조합은 같은 시드로 뽑힙니다.** 조합마다 시드가 달라지면 구도와 포즈가
> 함께 바뀌어 그림체 차이를 덮어 버리기 때문입니다. 기본 프롬프트는 메인 창의 것을 그대로
> 쓰며, 거기에 와일드카드처럼 매 장 달라지는 문법이 있으면 같은 이유로 알려 줍니다.

---

#### 3. 이상형 월드컵

두 그림을 나란히 놓고 마음에 드는 쪽을 고릅니다. 여기가 실제로 시간을 쓰는 곳이라, 손을
마우스로 옮기지 않고 계속 고를 수 있게 만들었습니다.

| 키 | 동작 | 키 | 동작 |
|---|---|---|---|
| `←` `→` | 왼쪽 / 오른쪽 승 | `Space` | 넘기기 (점수 변화 없음) |
| `↑` | 둘 다 좋음 | `Delete` | 양쪽 삭제 |
| `↓` | 둘 다 아쉬움 | 그림 클릭 | 크게 보기 |

이 키들은 **월드컵 탭 안이면 어디에 커서가 가 있든** 듣습니다. 방금 단추를 눌렀든 배율
슬라이더를 만졌든 상관없습니다. 다른 탭(조합 생성의 숫자 상자 등)과 메인 창에서는 원래대로
동작하니, 이 키들이 다른 화면을 방해하지는 않습니다.

`단축키 사용` 체크를 끄면 판정 키가 전부 멈추고, 방향키·`Space`·`Delete`는 슬라이더나
목록처럼 원래 받던 곳으로 돌아갑니다. 판정은 단추로만 하게 됩니다. 이 설정도 저장됩니다.

**그림을 누르면 확대됩니다 — 고르는 것이 아닙니다.** 자세히 보려고 눌렀다가 그 그림이
마음에 든 것으로 처리되면 곤란하므로, 고르는 것은 그림 아래 버튼과 방향키로만 합니다.

**화면 위쪽**

- **리그** — 겨룰 범위를 좁힙니다. `전체` · `즐겨찾기` · `미확정만`(티어가 아직 확정되지
  않은 것만).
- **배율** — 50%~250%. 고해상도 모니터에서 그림을 키워 봅니다. 배율을 올려도 창의
  최소 크기는 커지지 않으므로, 창을 작게 줄여 쓰던 중에 배율만 올려도 잘리지 않습니다.
- **그림 자동 보충** — 겨룰 그림이 떨어지면 대기 중인 조합의 그림을 자동으로 더 뽑습니다.
  크레딧을 쓰므로 **기본은 꺼짐**입니다.
- **단축키 사용** — 위 표의 판정 키를 켜고 끕니다 (**기본 켜짐**).
- **진행 상황** — `대결 N회 · 겨룰 수 있는 조합 N개 · 미확정 N개`가 늘 떠 있고, 전부
  확정되면 알려 줍니다. 다른 탭을 들락거리며 확인할 필요가 없습니다.

**카드마다 있는 버튼**

| 버튼 | 하는 일 |
|---|---|
| ★ 즐겨찾기 | 즐겨찾기 리그에 올라가고, 일괄 정리에서 제외됩니다 |
| 🔒 잠금 | 일괄 정리에서 제외되고, 전적이 없어도 교배 부모가 됩니다 |
| 가중치 리롤 | 작가는 그대로 두고 가중치만 새로 뽑습니다. **그림과 전적은 다시 시작합니다** — 가중치가 달라졌으면 사실상 다른 그림체라, 예전 전적을 물려주면 순위가 거짓말이 됩니다 |
| 복사 | 이 조합의 프롬프트를 클립보드로 |
| 삭제 | 조합을 지웁니다. 그림 파일은 기본적으로 남으며(지우도록 설정했다면 휴지통으로 갑니다), 조합 생성 탭의 `되돌리기`로 되살릴 수 있습니다 |

---

#### 4. 그림체 개선 (교배)

점수가 매겨진 상위 조합을 부모로 삼아 자식을 냅니다. 부모 후보는 **썸네일과 함께**
보여 줍니다 — 태그 문자열만 보고 고르라는 것은 불친절하니까요.

| 값 | 기본값 | 뜻 |
|---|---|---|
| 만들 자식 수 | 8 | 한 번에 만들 자식 조합 수 |
| 부모로 쓸 상위 조합 | 20 | 점수 상위 몇 개까지를 부모 후보로 볼지 |
| 돌연변이율 | 0.15 | 자식의 자리 하나가 명단의 다른 작가로 바뀔 확률 |
| 가중치 흔들기 | 0.15 | 물려받은 가중치를 위아래로 흔드는 폭 |

자식은 작가 구성뿐 아니라 **가중치도 물려받습니다.** 매 세대 가중치를 새로 뽑으면
"이 작가를 세게 쓴 게 좋았다"는 정보가 사라지기 때문입니다. 점수는 부모 평균에서 시작하고,
전적이 없으므로 몇 판이면 제자리를 찾습니다.

부모 후보는 **전적이 있거나 잠근 조합**입니다. 만든 자식은 그림이 없으므로, 조합 생성 탭으로
돌아가 `그림 생성`을 눌러야 겨룰 수 있습니다.

**정리(물갈이)** 는 `최소 대결 수`만큼 싸웠는데 `점수 기준`보다 낮은 조합을 한 번에 치웁니다
(기본: 5전 이상 · 960점 미만). 취향이 아닌 작가를 알게 됐다면 `이 작가가 낀 조합 지우기`로
관련 조합을 통째로 지울 수 있습니다.

> 한 번에 여러 개가 사라지므로 지우기 전에 대상 개수를 보여 주고 확인을 받습니다.
> 즐겨찾기와 잠근 조합은 조건에 맞아도 남고, 잘못 눌렀으면 조합 생성 탭의 `되돌리기`로
> 되살릴 수 있습니다.

---

#### 5. 통계

지금까지 알아낸 것을 한 화면에 모읍니다. 아레나를 돌리는 목적이 결국 여기 있습니다 —
마음에 든 조합을 실제 작업에 가져가는 것.

- **티어 시트** — 조합을 점수순으로 줄 세운 표. 세대 · 점수 · 전적 · 작가 구성을 봅니다.
  확정되지 않은 티어에는 `?`가 붙습니다 (`S?`).
- **작가 순위** — 상위 20명. 조합이 아니라 **작가 단위**로 취향을 알려 줍니다.
- **세대 분포** — 교배가 몇 세대까지 갔는지.

시트에서 조합을 고르고 `메인 프롬프트로 보내기`를 누르면 메인 창 프롬프트 앞에 붙습니다.
`복사`는 클립보드로 보냅니다.

| 버튼 | 지우는 것 | 남는 것 |
|---|---|---|
| 대전 기록 초기화 | 점수와 전적만 | 작가 명단 · **조합** · 그림 |
| 통계 초기화 | 점수 · 전적 · **조합 목록** · 티어 시트 · 세대 분포 | 작가 명단 |
| 안 쓰는 그림 정리 | 어떤 조합도 가리키지 않는 그림 파일 (휴지통으로) | — |

`대전 기록 초기화`는 조합을 남기고 점수만 0으로 돌립니다 — 같은 조합으로 순위를 다시 매기고
싶을 때 씁니다. 그래서 세대 분포와 티어 시트에는 줄이 그대로 남습니다. 표까지 비우려면
**`통계 초기화`**를 쓰세요.

`통계 초기화`를 누르면 무엇이 지워지고 무엇이 남는지 개수와 함께 보여 주고, **`기록
내보내기`로 먼저 백업하라고** 안내합니다. 실수로 눌렀어도 조합 생성 탭의 `되돌리기`로
되살아나지만(그림 파일도 기본값에서는 남습니다), 다른 삭제를 한 번 더 하면 그 기회는
사라집니다.

셋 다 **작가 명단은 남깁니다.** 점수만 새로 매기고 싶은데 애써 모은 후보까지 날아가면
곤란하기 때문입니다. 명단을 비우려면 작가 명단 탭의 `전체 삭제`를 쓰세요.

**결산 생성 — 순위대로 실제 그림 뽑기**

순위가 나왔으면 위에서부터 차례로 그림을 뽑아 한 벌로 보고 싶어집니다. 조합을 하나씩 골라
프롬프트에 붙이는 대신, 순위대로 큐를 만들어 한 번에 돌립니다.

| 항목 | 뜻 |
|---|---|
| `상위 N` | 순위 위에서부터 몇 개까지 올릴지 (1~200) |
| `조합당 M` | 조합 하나마다 몇 장을 뽑을지 (1~20) |
| `전적 없는 조합도` | 한 번도 안 싸운 조합까지 포함할지. **기본 꺼짐** |
| `결산 생성` | 큐를 만들어 생성을 시작합니다 |

- 1등을 M장 다 뽑고 2등으로 넘어갑니다 — 결과 폴더에 **등수 순서 그대로** 쌓입니다.
- 버튼 아래에 `대상 조합 N개 · 뽑을 그림 M장`이 늘 떠 있고, 누르면 한 번 더 확인합니다.
  크레딧을 쓰는 동작이기 때문입니다.
- **결과는 결과 폴더에 저장됩니다** — 아레나 폴더의 습작과 다른 자리입니다. 갤러리·결과
  미리보기·파일명 규칙·메타데이터까지 평소 생성과 똑같습니다.
- 프롬프트·모델·해상도·스텝은 **메인 창의 지금 설정**을 그대로 씁니다. 작가 블록만 조합마다
  갈아 끼웁니다 (넣을 위치는 조합 생성 탭의 설정을 따릅니다).
- 시드도 메인 창의 `시드 랜덤` 설정을 따릅니다. **꺼 두면 같은 시드로 그림체만** 바뀐 한 벌이
  나오고, 켜 두면 장마다 다른 그림이 나옵니다.
- 전적이 없는 조합을 기본으로 빼는 이유는, 1000점이 실력이 아니라 '모름'이기 때문입니다.
  결산에 섞이면 순위가 있는 것처럼 보입니다.
- 메인 생성이 도는 중에는 시작하지 않습니다 (반대도 마찬가지입니다).

**기록 내보내기 · 불러오기**

아레나 폴더는 하나뿐입니다. 다른 주제로 새로 시작하려면 지금까지 쌓은 점수와 그림을
덮어써야 했는데, 한 벌을 통째로 담아 두었다가 나중에 그대로 되살릴 수 있습니다.

| 버튼 | 하는 일 |
|---|---|
| `기록 내보내기...` | 작가 명단 · 조합 · 점수 · 전적과 **그림 파일까지 zip 하나**에 담습니다. 아레나 폴더는 그대로 둡니다 |
| `기록 불러오기...` | 담아 둔 zip을 읽어 **지금 기록을 갈아 끼웁니다.** 그림도 함께 돌아옵니다 |

- 파일 이름은 `arena-20260908-1430.zip`처럼 날짜를 붙여 제안합니다. 여러 벌을 모아 둬도
  언제 것인지 보입니다.
- zip 안은 `arena.json` + `images/`입니다. 폴더로 풀고 싶으면 탐색기에서 그냥 풀면 되고,
  풀어 둔 폴더를 다시 zip으로 묶어도 그대로 읽힙니다.
- **불러오면 지금 기록이 사라집니다.** 무엇이 사라지고 무엇이 들어오는지 개수를 보여 주고
  확인을 받습니다. `되돌리기`로는 복구되지 않으니, 지금 것을 남기려면 **먼저 내보내세요.**
- 생성이 도는 중에는 불러오지 않습니다 — 뽑던 그림이 갈아 끼우기 전 조합의 것이기 때문입니다.

> 남이 만든 zip이라도 아레나 폴더 밖에는 아무것도 쓰지 않습니다. 경로가 폴더를 벗어나는
> 항목과 이미지가 아닌 파일은 버립니다.

---

#### 점수는 어떻게 매겨지나

체스에서 쓰는 Elo 레이팅을 그대로 씁니다. 이기면 오르고 지면 내려가되, **강한 상대를
이겼을 때 더 많이 오릅니다.** 모든 조합은 1000점에서 시작합니다.

**K값 — 한 판이 점수를 얼마나 흔드는가**

| 치른 판수 | K | 뜻 |
|---|---|---|
| 0 ~ 4전 | 32 | 크게 움직여 빨리 제자리를 찾습니다 |
| 5 ~ 9전 | 24 | 점차 안정됩니다 |
| 10전 이상 | 16 | 잘 흔들리지 않습니다 → **티어 확정** |

10전을 채우면 티어가 확정됩니다. 그 전까지는 통계 탭에서 `?`가 붙어, 두 판 싸운 1300점을
확정된 S로 오해하지 않게 합니다.

**티어 경계**

| 티어 | 점수 | 대략의 뜻 |
|---|---|---|
| S | 1200 이상 | 취향에 가장 가까운 조합 |
| A | 1120 ~ 1199 | 교배 부모로 쓰기 좋은 구간 |
| B | 1040 ~ 1119 | 평균 위 |
| C | 960 ~ 1039 | 평균 근처 (시작점은 1000점) |
| D | 880 ~ 959 | 정리 대상 기본 구간 |
| F | 880 미만 | 취향이 아닌 쪽 |

**대진**은 무작위가 아닙니다. 가장 덜 싸운 조합을 먼저 올리고, 그 상대는 점수가 가까운 후보
중에서 역시 덜 싸운 쪽을 고릅니다. 승부가 뻔한 대결이 줄어 같은 클릭 수로 순위가 더
정확해집니다. 직전과 같은 짝이 연달아 나오지도 않습니다.

**작가 점수**는 조합의 점수 변화량을 **자리 가중치에 비례해** 나눠 받습니다.
`1.5::artist:a::`와 `0.5::artist:b::`로 이뤄진 조합이 20점을 얻었다면 `a`가 15점, `b`가
5점을 받습니다 — 세게 들어간 작가가 결과에 더 책임이 있으니까요.

---

#### 파일이 저장되는 곳

아레나 데이터는 일반 결과물과 **따로** 둡니다. 아레나가 뽑는 그림은 습작이라, 결과 폴더에
섞이면 갤러리가 습작으로 뒤덮입니다.

| 경로 | 내용 |
|---|---|
| `artist_arena/arena.json` | 작가 명단 · 조합 · 점수 · 세대 |
| `artist_arena/images/` | 조합마다 뽑은 그림 |

기본 위치는 앱 데이터 폴더 아래이고, **옵션 → 폴더 경로**에서 바꿀 수 있습니다.
**폴더 메뉴 → 그림체 아레나 폴더**로 탐색기에서 바로 엽니다. 그림은 한 장 뽑을 때마다 곧바로
기록되므로, 도중에 앱이 꺼져도 이미 쓴 크레딧이 날아가지 않습니다.

---

#### 막히기 쉬운 곳

| 증상 | 이유와 해결 |
|---|---|
| 월드컵에 그림이 안 뜬다 | 겨루려면 **그림이 있는 조합이 2개 이상** 필요합니다. 조합 생성 탭에서 `그림 생성`을 먼저 누르세요 |
| `그림 생성`을 눌러도 시작되지 않는다 | 메인 창에서 생성이 돌고 있으면 아레나가 시작되지 않습니다 (반대도 같습니다). NovelAI는 계정당 요청을 하나씩 보내는 것이 규칙이라 순서를 지킵니다 |
| 가중치 범위를 바꿨는데 옛 값이 보인다 | 규칙은 **새로 만드는 조합**에만 적용됩니다. `랜덤 조합 만들기`를 다시 누르면 미리보기에 새 값이 뜹니다. 작가 명단에서 가중치를 고정해 둔 작가는 범위를 무시합니다 |
| 조합을 만들었는데 개수가 모자라다 | 작가 명단이 작아 더 만들 조합이 없는 경우입니다. 같은 작가 구성은 두 번 만들지 않습니다 — 작가를 더 넣으세요 |
| 교배 버튼이 눌리지 않는다 | 전적이 있거나 잠근 조합이 **2개 이상** 있어야 합니다. 월드컵을 몇 판 돌린 뒤 다시 오세요 |
| 티어가 계속 `?`다 | 그 조합이 아직 10전을 채우지 못했습니다. 리그를 `미확정만`으로 두면 그 조합들이 먼저 올라옵니다 |
| 그림마다 배경·포즈가 다르다 | 기본 프롬프트에 와일드카드나 `<a\|b>` 같은 문법이 있습니다. 조합 생성 탭에 경고가 떠 있을 겁니다 — 빼고 다시 뽑으세요 |
| 아레나 창의 윗부분이 잘린다 | 창 바닥의 `창 크기 초기화`를 누르세요. 기본 크기로 화면 가운데에 놓고 배율도 100%로 되돌립니다 |
| 지웠는데 폴더에 그림이 남는다 | 기본 동작입니다 — 되돌릴 수 있게 파일을 남깁니다. 통계 탭의 `안 쓰는 그림 정리`로 치우거나, 조합 생성 탭의 `그림 파일도 휴지통으로`를 켜세요 |
| 실수로 조합을 지웠다 | 조합 생성 탭의 `되돌리기`를 누르세요. 마지막 삭제 한 번을 되살립니다 (월드컵·진화 탭에서 지운 것도 포함) |
| 그림 파일까지 지워 버렸다 | **휴지통을 확인하세요.** 아레나는 그림을 바로 지우지 않고 휴지통으로 보냅니다. 되살린 뒤 아레나 폴더의 `images/`에 같은 이름으로 돌려놓으면 조합이 다시 그림을 찾습니다 |

---

#### 생성 진행 상황

창 아래 **상태줄**에 진행 막대와 한 줄 설명이 뜹니다. 탭 밖에 있어서 월드컵이나 통계를 보고
있어도 어디까지 갔는지 보입니다.

| 표시 | 뜻 |
|---|---|
| `N장 생성을 시작합니다 (시드 …)` | 큐에 넣은 장수와 이번 세션의 시드 |
| `생성 중 3/12 — 1.2::artist:…` | 지금 뽑고 있는 조합 |
| `완료 3/12 — 남은 9장` | 방금 한 장이 저장됐습니다 |
| `재시도 대기 3/12 — … (5초 후, 1번째)` | 일시적 오류로 같은 장을 다시 시도합니다 |
| `다음 장(4/12)까지 3초 대기` | 요청 간 간격을 지키는 중입니다 |

`중지`를 누르면 다음 장부터 멈춥니다. 이미 뽑은 그림은 그대로 남습니다.

#### 알아 두면 좋은 것

- 화면에서 만진 값은 전부 설정에 남아 다음 실행에 복원됩니다.
- 아레나 생성은 **메인 생성과 같은 줄에 섭니다.** 메인 배치가 도는 중에는 아레나가 시작되지
  않고, 반대도 마찬가지입니다.
- 창을 닫아도 진행 중인 생성은 멈추지 않습니다 (멈추려면 `중지`를 누릅니다).
- 아레나가 뽑은 그림은 갤러리·결과 미리보기에 끼어들지 않습니다.
- 창은 **최소화·최대화**할 수 있습니다 — 그림을 크게 놓고 고를 때 씁니다
  (→ [곁창 다루기](#곁창-다루기-최소화--최대화)).

---

### 세팅별 연속 생성

여러 개의 세팅 파일을 순서대로 순환하며 연속 생성하는 기능입니다 (V4 기능 이식). 프롬프트, 모델,
해상도 등이 완전히 다른 설정을 하나의 배치에서 처리할 때 유용합니다.

#### 사용 방법

1. **세팅 파일 준비**: `Files` → `Save Settings`(`Ctrl+S`)로 원하는 설정을 각각 파일로 저장합니다
   (**2개 이상** 필요, `.json`/`.txt` 모두 가능)
2. **`세팅별 연속 생성` 버튼 클릭**: 파일 선택 대화상자가 열립니다
3. **세팅 파일 2개 이상 선택** (Ctrl 또는 Shift로 다중 선택) — 1개만 고르면 경고와 함께 취소됩니다
4. 생성 바의 **매수** · **간격** 입력란 값 그대로 시작합니다 (매수 0 = 무제한, 중지 버튼으로 종료)

#### 동작 방식

- 이미지 1장을 생성하기 직전에 다음 세팅 파일을 자동으로 불러와 화면에 적용합니다
- 선택한 파일을 **순서대로 순환**합니다 (마지막 파일 다음엔 다시 첫 번째 파일로)
- 옵션 → 생성의 **`세팅별 연속 생성에서 세팅 파일을 무작위 순서로`** 를 켜면 순서 대신
  매번 무작위로 고릅니다 (같은 파일이 연달아 나오지는 않습니다)
- 각 세팅 파일로 생성된 이미지는 **세팅 파일명과 같은 이름의 하위 폴더**에 저장됩니다
- 중지 버튼을 누르면 진행 중인 이미지까지만 완료하고 순환을 멈춥니다

#### 저장 폴더 구조 예시

세팅 파일 `A.json`, `B.json`을 골라 4장을 생성하면:

```
results/
├── A/          ← A.json 세팅으로 생성된 이미지 (1, 3번째)
└── B/          ← B.json 세팅으로 생성된 이미지 (2, 4번째)
```

#### 활용 예시

**캐릭터별 대량 생성** — 각 캐릭터의 설정을 세팅 파일로 따로 저장한 뒤 세팅별 연속 생성으로
모든 캐릭터를 균등하게 생성합니다 (`character_A.json`, `character_B.json` → A→B→A→B... 순).

**해상도별 비교 생성** — 같은 프롬프트를 다른 해상도로 저장한 세팅 파일을 사용해 결과를
비교합니다.

---

### 프리셋

`Ctrl+P`로 프리셋 관리 창을 엽니다. 모델·해상도·샘플러·스텝·프롬프트 등 생성 파라미터를 이름
붙여 저장하고 목록에서 골라 불러올 수 있습니다. 설정 파일(Save/Load Settings)과 달리 이름으로
관리되는 전용 폴더(프리셋 폴더)에 저장됩니다.

프리셋을 지우면 파일이 **휴지통으로** 갑니다. 앱 안에는 되돌리기가 없으므로, 실수로 지웠다면
휴지통에서 되살려 프리셋 폴더에 돌려놓으면 그대로 다시 뜹니다.

---

### 갤러리 뷰

`F3`으로 결과 저장 폴더의 썸네일 격자를 봅니다. 새 이미지가 생성될 때마다 자동으로 추가됩니다.

---

### WD14 자동 태깅

`Ctrl+T`로 이미지에서 태그를 예측해 프롬프트에 붙이는 기능을 엽니다. ONNX 모델 파일은 용량 때문에
앱에 동봉되지 않으므로, 처음 한 번은 모델을 받아야 합니다.

#### 모델 준비 (옵션 → 폴더 경로)

1. **`모델 폴더`** — 모델을 둘 폴더입니다. 기본값은 데이터 폴더의 `wd14/`이고, 이미 모델을
   받아 둔 폴더가 있다면 그 폴더를 그대로 가리켜도 됩니다
2. **`태거 모델`** — 쓸 모델을 고릅니다. 폴더에 이미 있는 모델은 `(설치됨)`으로 표시됩니다
3. **`모델 다운로드`** — 고른 모델을 HuggingFace에서 받습니다 (v3 모델 약 400MB, 진행률이
   뜨고 취소할 수 있습니다). 받는 동안 앱은 그대로 쓸 수 있습니다

파일 이름은 `<모델 이름>.onnx` + 같은 이름의 `.csv`가 기본이고, `model.onnx` +
`selected_tags.csv`로 받아 둔 폴더도 인식합니다. 둘 다 아니면 폴더 안의 첫 번째
`.onnx` / `.csv`를 씁니다.

#### 태그 뽑기

이미지를 창에 끌어다 놓거나 `찾아보기`로 고르면 태그와 확률이 뜹니다. 필요한 것만 체크한 뒤
`태그 넣기`를 누르면 현재 프롬프트 뒤에 붙고, `클립보드에 복사`를 누르면 같은 내용(쉼표 구분)이
클립보드로 들어갑니다 — 이때는 창이 닫히지 않으므로 다른 곳에 붙여넣고 이어서 작업할 수 있습니다. 등급(rating) 태그는 프롬프트에 쓸 것이
아니라 목록에서 빠지고, 캐릭터 태그는 더 엄격한 기준(0.85)을 넘어야 나옵니다.

---

### 프롬프트 어시스턴트 (`Ctrl+G`)

WD 태거 · LLM 태거 · LLM 어시스턴트 세 모드를 한 창에서 씁니다. 자세한 지시문은 아래
[LLM 프롬프트 세트](#llm-프롬프트-세트-지시문-직접-고치기)에서 바꿉니다.

이 창은 **곁창(모드리스)** 입니다. 떠 있는 동안에도 메인 창에서 프롬프트를 고치고 생성을
돌릴 수 있고, `프롬프트에 이어붙이기` · `프롬프트 덮어쓰기`를 눌러도 창이 닫히지 않습니다 —
결과를 메인 창에 넣어 보고, 마음에 안 들면 그 자리에서 다시 생성해 덮어쓰면 됩니다. 창은
`닫기` 버튼(또는 `Esc`)으로 닫습니다. 메인 창을 닫으면 함께 닫힙니다.

---

### LLM 프롬프트 세트 (지시문 직접 고치기)

프롬프트 어시스턴트가 LM Studio의 로컬 LLM에게 **어떻게 써 달라고 지시하는지**를 담은 문장들을
JSON 파일 하나로 빼낼 수 있습니다. 모델마다 잘 듣는 문구가 달라서, "태그를 더 짧게", "한국어
고유명사는 영어로" 같은 취향은 코드를 고치지 않고 이 파일에서 바꿉니다.

#### 만들기

`옵션 → LLM` 아래쪽 **`프롬프트 세트 (JSON 파일)`**에서:

1. **`기본 파일 만들기`** — 지금 쓰고 있는 기본 문안이 그대로 든 파일을 만듭니다. 경로 칸이
   비어 있으면 데이터 폴더의 `llm_prompts.json`에 만듭니다. **이미 파일이 있으면 덮어쓰지
   않습니다** (편집본이 날아가지 않게)
2. **`편집기로 열기`** — 그 파일을 메모장 등 OS 기본 편집기로 엽니다
3. 고치고 저장하면 다음에 어시스턴트 창(`Ctrl+G`)을 열 때부터 적용됩니다
4. **`찾아보기...`** — 파일을 여러 벌 만들어 두고 골라 쓸 수 있습니다 (모델별 세트 등)

경로 칸을 **비워 두면** 앱에 내장된 기본 문안을 씁니다. 경로 아래 문구가 지금 그 파일이
읽히는지(`프롬프트 세트를 읽었습니다: ...`) 아니면 어디가 잘못됐는지 바로 알려 줍니다.

#### 파일 안의 항목

고치고 싶은 항목만 남기고 나머지는 지워도 됩니다 — **적어 둔 항목만** 기본값 위에 덮입니다.
그래서 앱이 올라가며 항목이 늘어도 예전 파일이 깨지지 않습니다.

| 항목 | 언제 쓰이나 |
|---|---|
| `styles.natural.system_prompt` | 출력 스타일이 `자연어 프롬프트`일 때의 역할 지시 |
| `styles.danbooru.system_prompt` | 출력 스타일이 `단부루 태그`일 때의 역할 지시 |
| `styles.<...>.output_kind` | 사용자 메시지의 `{kind}` 자리에 들어갈 말 |
| `common_rules` | 두 스타일 공통 규약 (JSON으로만 답하기 등) |
| `assistant_instruction` | `LLM 어시스턴트` 모드(이미지 + 변형 지시)에서 덧붙는 지시 |
| `length_hints.short` / `medium` / `long` | 출력 길이 라디오에 해당하는 분량 지시 |
| `user_messages.*` | 시스템 프롬프트와 함께 보내는 사용자 메시지 (`text_only` / `image_only` / `image_with_text`, 어시스턴트용은 `assistant_` 접두사) |

`user_messages`의 `{kind}`와 `{text}`만 값으로 바뀌고, 그 밖의 중괄호는 적은 그대로 갑니다
(`{"prompt": "..."}` 같은 JSON 예시를 그대로 써도 됩니다).

옵션의 **`시스템 프롬프트`** 칸은 이 파일과 별개로 남아 있고, 조립된 지시문 **맨 뒤에** 덧붙습니다.

> 파일이 깨졌거나(JSON 오류) 경로가 틀리면 어시스턴트 창을 열 때 이유를 알려 주고 **내장 기본
> 문안으로 계속 진행**합니다 — 파일 하나 때문에 기능이 막히지는 않습니다.

---

### 이미지에서 설정 불러오기

NovelAI로 생성된 PNG/WEBP 이미지는 메타데이터를 담고 있어 설정을 그대로 복원할 수 있습니다.

- **메뉴 사용**: `도구` → 이미지 정보 열기 — 파일 선택 창 없이 **이미지 정보 창이 먼저** 뜹니다.
  창 안에서 `파일 열기`를 누르거나 이미지를 끌어다 놓으면 됩니다.
- **드래그 앤 드롭**: 이미지를 메인 화면이나 **프롬프트 입력란**에 끌어다 놓기 — 어느 쪽이든
  같은 창이 뜹니다 (프롬프트에 파일 경로가 붙지 않습니다).

창은 모드리스라 메인 화면을 막지 않습니다. 왼쪽에 프롬프트·네거티브·캐릭터 프롬프트 전문과
시드·스텝·Prompt Guidance·샘플러·크기가 항목별 체크박스로 뜨고, 오른쪽에는 원본 이미지가
확대/축소(휠, 더블클릭으로 원래 크기) 가능한 미리보기로 붙습니다.

`선택 항목 적용`은 **체크한 항목만** 화면에 되돌려 놓습니다 — 프롬프트만 가져오고 시드는
지금 값을 쓰는 식으로 고를 수 있습니다. `선택 항목 복사`는 같은 내용을 클립보드에 넣습니다.

> **웹을 거친 이미지도 읽습니다.** 커뮤니티·SNS에 올렸다 내려받은 PNG는 용량 최적화 과정에서
> 텍스트 청크가 이미지 데이터 뒤로 밀리거나, 압축(zTXt)되거나, EXIF로 옮겨 가는 일이 있습니다.
> 이런 파일도 정보가 남아 있으면 그대로 읽습니다.

---

### 로그 뷰어 / 새 버전 확인

- **로그 뷰어** (`Ctrl+L`): 일반/크래시 로그 확인, 레벨 필터, 디버그 로그 토글. 콘솔 없이 실행해도
  네이티브 크래시와 처리되지 않은 예외가 파일에 남습니다
- **새 버전 확인** (기타 메뉴): 배포된 최신 릴리스와 현재 버전을 비교합니다. 시작할 때 자동
  확인은 옵션 → 인터페이스에서 끌 수 있고, 새 버전이 있을 때만 상태바에 알립니다

---

## 와일드카드 시스템

`wildcards` 폴더(옵션 → 폴더에서 경로 지정)의 `.txt` 파일에서 한 줄을 골라 프롬프트에 넣는
기능입니다. 하위 폴더로 나눠 정리할 수 있고, `#`으로 시작하는 줄은 주석으로 무시됩니다.

| 문법 | 뜻 |
|---|---|
| `__folder/file__` | 파일에서 한 줄 무작위 선택 |
| `__=folder/file__` | 공유 랜덤 — 같은 이미지를 만드는 동안(메인 프롬프트·캐릭터 프롬프트 등 어디에 써도)에는 항상 같은 값 |
| `##folder/file##` | 루프카드 — 이미지가 넘어갈 때마다 파일의 다음 줄로 순차 진행 (끝까지 가면 처음으로) |
| `##folder/file*3##` | 루프카드 반복 — 같은 값을 3장 동안 쓴 뒤 다음 줄로 진행 |

파일 위치는 `Folders` 메뉴로 바로 열 수 있습니다. 이름을 외울 필요는 없습니다 — 프롬프트
입력란에 `__`(또는 `__=` · `##`)를 치면 폴더 안 목록이 자동완성으로 뜨고, 고르면 닫는
기호까지 붙습니다 ([자동완성](#자동완성-태그--와일드카드) 참고).

**예시** (`characters.txt`):
```
1girl, blue eyes, long hair
1boy, red hair, short hair
1girl, green eyes, ponytail
```
프롬프트에 `__characters__, standing, outdoors`라고 쓰면 매번 다른 줄이 무작위로 들어갑니다.

---

## 화면 레이아웃 조절

- **좌우 폭 조절**: 왼쪽 입력 패널과 오른쪽 결과 패널 사이의 경계선을 드래그하면 폭 비율을
  바꿀 수 있습니다. 결과 패널이 넓어지거나 좁아지는 만큼 입력 패널의 나머지 요소(캐릭터
  프롬프트, Options & Settings 등)도 함께 따라 늘고 줄어듭니다. 비율은 종료 시 저장되어
  다음 실행에도 그대로 유지됩니다
- 입력 패널은 **기본 폭의 절반(230px)까지** 좁힐 수 있습니다. 좁히면 생성 버튼·퀵 매수 버튼,
  해상도 비율 버튼, 캐릭터 프롬프트 상단 버튼이 한 줄에 다 들어가지 않는 만큼 **다음 줄로
  넘어갑니다** — 잘리거나 사라지지 않습니다. 결과 이미지를 크게 보면서 작업할 때 씁니다
- **프롬프트 ↔ 캐릭터 프롬프트 비율**: 두 영역 사이의 구분선을 드래그해 세로 비율을 조절합니다
  (역시 세션 간 유지됩니다)
- 창 크기·위치, 프롬프트/캐릭터 슬롯 입력란 높이도 함께 기억합니다. **v0.7.3 이전에는
  Windows에서 이 값들이 저장되지 않았습니다** — 0.7.3에서 처음 실제로 동작하므로, 한 번 더
  맞춰 두면 그 다음 실행부터 복원됩니다
- **입력란 높이**: 프롬프트·캐릭터 프롬프트 각 입력란 아래 드래그 핸들로 높이를 조절합니다
- **입력 패널 좁게** (`F9`): 보기 메뉴에서 켜면 입력 패널이 최소 폭(230px)으로 접히고, 다시
  누르면 접기 직전 폭으로 돌아갑니다. 스플리터를 매번 끌지 않아도 결과 이미지를 크게 볼 수 있습니다
- 보기 메뉴의 `레이아웃 초기화` (`Ctrl+R`)로 모든 비율을 기본값으로 되돌릴 수 있습니다

### 곁창 다루기 (최소화 · 최대화)

메인 창 말고 따로 뜨는 창들 — **그림체 아레나** · 갤러리 뷰 · 이미지 정보 · 프롬프트
어시스턴트 · 로그 뷰어 · 마스크 편집기 — 도 제목 표시줄에서 **최소화 · 최대화**할 수 있습니다.

- **최대화 단추**가 생기면서 **제목 표시줄 더블클릭**과 Windows 스냅(`Win`+`←`/`→`, 창을
  화면 가장자리로 끌기)도 함께 동작합니다. 그림을 크게 보는 월드컵 탭에서 특히 편합니다
- 그림체 아레나에는 창 바닥(닫기 옆)에 **`창 크기 초기화`**가 있습니다. 저장된 창 위치·크기를
  지우고 기본 크기로 화면 가운데에 놓으며, 월드컵 배율도 100%로 되돌립니다. 창이 화면 밖으로
  나가 윗부분이 잘려 보일 때 쓰세요 — 어느 탭에서든 누를 수 있습니다
- 로그 뷰어와 마스크 편집기는 **최소화가 없습니다.** 이 둘은 떠 있는 동안 메인 창이 입력을
  받지 않는 창이라, 최소화해 버리면 되돌아갈 방법이 없어집니다
- 창 위치가 **화면 밖으로 나가 있으면 열 때 안으로 끌어옵니다.** 모니터를 떼거나 해상도를
  바꿔 제목 표시줄이 화면 위로 넘어가면 창을 끌 수도 닫을 수도 없기 때문입니다
- **전체 화면(`F11`)은 없습니다.** 크게 보는 것은 최대화를 쓰세요. Windows에서 곁창을
  전체 화면으로 만들면 창틀 높이만큼 내용이 위로 밀려 탭 줄이 잘리는 문제가 있어 넣지
  않았습니다. 메인 창의 `F11`은 그대로 결과 패널 토글입니다

---

## 파일이 저장되는 곳

경로는 OS 표준 위치를 따르며 첫 실행 때 자동으로 만들어집니다. 결과 · 와일드카드 · 프리셋 · 조합
폴더는 옵션에서 원하는 위치로 바꿀 수 있습니다.

**데이터 폴더** — Windows `%LOCALAPPDATA%\NAI-Auto-V5\NAI-Auto-V5` ·
Linux `~/.local/share/NAI-Auto-V5` · macOS `~/Library/Application Support/NAI-Auto-V5`

| 항목 | 위치 |
|---|---|
| 결과 이미지 | 데이터 폴더의 `results/` |
| 와일드카드 | 데이터 폴더의 `wildcards/` |
| 프리셋 | 데이터 폴더의 `presets/` |
| 아티스트 조합 | 데이터 폴더의 `artist_combos/` |
| 그림체 아레나 | 데이터 폴더의 `artist_arena/` (`arena.json` + `images/`) |
| WD14 모델 | 데이터 폴더의 `wd14/` (옵션 → 폴더 경로에서 폴더 지정·모델 다운로드) |
| 설정 | 설정 폴더의 `settings.json` |
| 창 크기·패널 폭 | Windows 레지스트리 `HKCU\Software\sagawa8b\NAI-Auto-V5` (Linux/macOS는 OS 표준 설정 위치) |
| 로그 | OS 표준 로그 폴더 (로그 뷰어의 `폴더 열기`로 바로 이동) |
| API 토큰 | OS 키링 (파일로 저장하지 않음) |

---

## 단축키

| 키 | 기능 | 키 | 기능 |
|---|---|---|---|
| `Ctrl+I` | 로그인 | `Ctrl+Shift+I` | API 계정 관리 |
| `Ctrl+S` / `Ctrl+O` | 설정 저장 / 불러오기 | | |
| `Ctrl+,` | 옵션 | `F2` | i2i / 인페인팅 패널 토글 |
| `Ctrl+P` | 프리셋 | `F3` | 갤러리 뷰 |
| `Ctrl+T` | WD14 자동 태깅 | `F4` | 강화 업스케일 패널 토글 |
| `Ctrl+G` | 프롬프트 어시스턴트 | | |
| `Ctrl+L` | 로그 보기 | `F11` | 결과 패널 토글 |
| `Ctrl+R` | 레이아웃 초기화 | `F8` | 프롬프트 결과 표시 토글 |
| `F5` | 결과 폴더 열기 | `F9` | 입력 패널 좁게 토글 |
| `F6` | 와일드카드 폴더 열기 | `F7` | 프리셋 폴더 열기 |

---

## 아직 안 되는 기능

V4에는 있지만 V5에는 아직 없는 기능입니다.

- **Character Reference / Vibe Transfer / Precise Reference / Curated Inpainting** — NovelAI가
  V5 모델로는 아직 내놓지 않은 기능입니다. 출시되면 이식할 예정입니다
- **아티스트 조합 편집 대화상자** — 지금은 조합 폴더의 JSON 파일을 텍스트 편집기로 직접
  만들어야 합니다

---

## 문제 해결

| 증상 | 원인 / 대처 |
|---|---|
| 로그인이 안 됨 | 토큰이 `pst-`로 시작하는지 확인. 토큰이 만료됐다면 NovelAI 웹에서 새로 발급 |
| "Anlas 부족" 오류 | 무료 티어를 넘는 해상도이거나 크레딧이 부족한 경우 — 해상도를 낮추거나 구독 확인 |
| 네트워크 오류 | 인터넷 연결 확인. 일시적 오류는 앱이 자동으로 몇 차례 재시도합니다 |
| 와일드카드가 적용되지 않음 | 파일 경로·확장자(`.txt`) 확인, 옵션 → 폴더의 와일드카드 경로가 맞는지 확인 |
| 세팅별 연속 생성 버튼이 비활성 | 로그인 상태가 아니거나 이미 다른 생성이 진행 중인 경우 |
| 태그 자동완성이 안 뜸 | 옵션 → 자동완성에서 `태그 자동완성 사용`과 태그 DB 경로 확인 |
| 와일드카드 자동완성이 안 뜸 | 옵션 → 자동완성의 `와일드카드 자동완성 사용`이 켜져 있는지, 옵션 → 폴더 경로의 와일드카드 폴더에 `.txt` 파일이 있는지 확인 (태그 자동완성과는 별개 스위치입니다). 폴더 경로를 바꿨다면 다음 실행부터 적용됩니다 |
| WD14 자동 태깅이 안내만 뜨고 끝남 | 모델이 아직 없는 경우 — 옵션 → 폴더 경로에서 모델을 고르고 `모델 다운로드`를 누르거나, 이미 받아 둔 폴더를 지정 |
| 모델을 받았는데도 "onnxruntime을 쓸 수 없습니다" | 소스로 실행 중이면 `pip install -e .`(또는 `pip install onnxruntime numpy`), 배포본이면 v0.6.7 이상을 받으세요 — 그 전 배포본에는 추론 런타임이 들어 있지 않습니다 |
| 그림체 아레나 관련 문제 | [그림체 아레나](#그림체-아레나) 절의 *막히기 쉬운 곳* 표를 보세요 |
| 작업 표시줄 아이콘이 이상함 | 0.6.5 이전 버전의 문제였습니다 — 최신 버전으로 업데이트하세요 |

문제가 지속되면 로그 뷰어(`Ctrl+L`)에서 로그를 확인하거나, 디버그 로그를 켠 뒤 재현해 로그
파일을 함께 첨부해 문의하세요.

---

## 원작 / 라이선스

원작: [DCP-arca/NAI-Auto-Generator](https://github.com/DCP-arca/NAI-Auto-Generator) ·
전신: [NAI-Auto-V4](https://github.com/sagawa8b/NAI-Auto-V4) ·
stealth PNG 리더: [neggles/sd-webui-stealth-pnginfo](https://github.com/neggles/sd-webui-stealth-pnginfo)

이 저장소는 [PolyForm Noncommercial 1.0.0](LICENSE) — **비상업적 용도로만** 사용할 수 있습니다.

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nai-auto-v5"
version = "0.7.2"
description = "NovelAI Diffusion V5 image generation automation app (PySide6)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "PolyForm-Noncommercial-1.0.0" }
dependencies = [
    "PySide6>=6.7",
    "requests>=2.28.0",
    "argon2-cffi>=21.3.0",
    "keyring>=24.0",
    "Pillow>=10.0",
    "pydantic>=2.0",
    "platformdirs>=4.0",
    # WD14 자동 태깅 — 이 둘이 빠지면 배포본에서 기능 자체가 뜨지 않는다
    # (v0.6.5까지 선택 의존성이라 exe로 받은 사용자는 쓸 수 없었다).
    "numpy>=1.24",
    "onnxruntime>=1.17",
    # 자연어 프롬프트 생성 — LM Studio 로컬 LLM 연동. 연결 확인 API와 동기 타임아웃이
    # 1.5.0부터라 그 이상을 요구한다. WD14와 같은 이유로 필수 의존성이다
    # (선택 의존성이면 exe 사용자가 쓸 수 없다).
    "lmstudio>=1.5.0",
]

[project.optional-dependencies]
dev = ["ruff>=0.5"]

[project.gui-scripts]
nai-auto-v5 = "naiauto.ui.app:main"

[project.scripts]
nai-auto-v5-smoke = "naiauto.smoke:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
naiauto = ["resources/languages/*.json", "resources/tags/*.csv"]

[tool.ruff]
line-length = 110
target-version = "py310"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]
